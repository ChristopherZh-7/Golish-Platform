{
  "$schema": "../../../docs/design/2026-06-03-two-level-phase-stage-model.md#5",
  "$comment": "Two-level phase grouping over every StageKind. Application Understanding and Investigation are schema-neutral members of the vuln phase and remain absent from all current production profiles; the server-frozen topology graph selects either the historical Candidate/Verification pair or the unified Investigation path. Static entry_approval remains risk/compatibility metadata; routine runtime confirmation belongs to Scoping, and post-Scoping crossings auto-advance only after typed barriers and deterministic Gates.",

  "phases": [
    { "id": "prep",         "stages": ["scoping", "target_intel"] },
    { "id": "active_recon", "stages": ["external_attack_surface", "enumeration"], "entry_approval": "active_scan" },
    { "id": "vuln",         "stages": ["vuln_triage", "application_understanding", "investigation", "attack_candidate", "verification"], "entry_approval": "exploit_validation" },
    { "id": "post_exploit", "stages": ["access_validation", "internal_discovery", "objective_pathing", "objective_simulation"] },
    { "id": "closeout",     "stages": ["reporting", "cleanup"] }
  ]
}
