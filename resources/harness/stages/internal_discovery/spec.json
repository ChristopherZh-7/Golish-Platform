{
  "$schema": "../../../../docs/design/2026-05-26-stage-harness-mvp-external-attack-surface.md#4.1",
  "$comment": "Stage spec: internal_discovery. L5 red-team internal recon after foothold. Approval-gated. Phase A.",

  "id": "internal_discovery",
  "kind": "internal_discovery",
  "risk_level": "critical",

  "requires_stages": ["access_validation"],
  "allowed_next_stages": ["objective_pathing"],

  "allowed_tool_types": ["post-exploit", "recon/port-scan", "network/tunnel"],

  "deliverable_schema": "StageDeliverable",
  "gate_validator": "validate_stage_gate",

  "gate_rules": [
    {
      "op": "for_all",
      "over": "claims",
      "require": { "pred": "non_empty", "field": "evidence_ids" },
      "on_fail": { "reason": "every claim must cite evidence", "hints": ["add evidence to each claim via prior tool calls"] }
    },
    {
      "op": "for_all",
      "over": "findings",
      "require": { "pred": "non_empty", "field": "evidence_refs" },
      "on_fail": { "reason": "every finding must cite evidence", "hints": ["add evidence to each finding via prior tool calls"] }
    }
  ],

  "min_invocations": {},

  "max_other_skips": 1,

  "human_approval": {
    "required_before": ["post_exploit", "exploit_validation"]
  },

  "agent_continuity": "multi_session_relay",

  "inherits_evidence_from": [
    {
      "stage_kind": "access_validation",
      "evidence_kinds": ["foothold"]
    }
  ]
}
