{
  "$schema": "../../../../docs/design/2026-05-26-stage-harness-mvp-external-attack-surface.md#4.1",
  "$comment": "Stage spec: external_attack_surface — DEFINE the attack surface (liveness + port scan + service fingerprint + HTTP probe + screenshots). 阶段重排 2026-06-09: 端口/服务前移到此 (先定义 host×端口×服务×活web); JS/API 移交 enumeration. 见 docs/design/2026-06-09-active-stage-verify-first.md.",

  "id": "external_attack_surface",
  "kind": "external_attack_surface",
  "risk_level": "medium",

  "requires_stages": ["scoping", "target_intel"],
  "allowed_next_stages": ["enumeration", "reporting"],

  "$comment_allowed_tool_types": "These are backend authorization taxonomy selectors, not the model tool surface. The Prober sees only eas_probe_http_liveness, eas_discover_ports, eas_fingerprint_services and eas_fingerprint_web_stack; raw httpx/naabu/masscan/nmap/WhatWeb/pentest_run remain hidden behind fixed guarded recipes.",
  "allowed_tool_types": ["recon/port-scan", "recon/http", "recon/visual"],

  "deliverable_schema": "ExternalAttackSurfaceDeliverable",
  "gate_validator": "validate_external_attack_surface_gate",

  "$comment_findings_allowed": "Attack-surface mapping stage: the deliverable is liveness/port/service coverage, NOT vulnerabilities. submit_stage_deliverable drops any findings submitted here and tells the model to put discoveries in claims. Design 2026-06-15-recon-stage-findings-suppression.",
  "findings_allowed": false,

  "$comment_host_aware": "EAS splits exact-name/origin work from network work. Domain/URL rows own HTTP Host/SNI LIVENESS and exact-origin WEB. Explicit IP rows own PORT then per-open-port SERVICE. Explicit CIDR rows own only range LIVENESS/PORT; guarded in-range child IPs enter a supplemental wave and own SERVICE/WEB. DNS/real_ip never authorize an IP. Wildcard pattern rows are passive authorization only and all EAS techniques are N/A.",
  "host_aware_coverage": true,

  "$comment_freshness_window": "Phase E activation (design 2026-06-22 Phase D): gray-switch ON. The EAS DB-truth back-fill (GOLISH-EAS-LIVENESS/PORT/SERVICE-FINGERPRINT/WEB-FINGERPRINT) only counts data collected this stage-run (targets.liveness_checked_at/ports_scanned_at + fingerprints.detected_at >= stage start; migration 20260623000001). EAS coverage_complete now uses authoritative DB/ledger found facts, so self-reported found cells cannot fake active surface coverage. Default false = presence-only.",
  "freshness_window": true,

  "$comment_asset_wave_barrier": "Stage expansion wave barrier (updated 2026-07-06): freeze the current EAS coverage denominator to the active durable stage_asset_wave item list, falling back to operation_state.stage_started_at only when no durable wave exists. Assets discovered during the wave are persisted and shown as next_wave_pending, but do not block the current wave's gate. After all current org waves pass, stage_run queues supplemental durable waves for the newly DB-entered assets; the next stage_run call processes only those delta wave assets before the stage can close.",
  "asset_wave_barrier": true,

  "gate_rules": [
    {
      "op": "for_all",
      "over": "claims",
      "require": { "pred": "non_empty", "field": "evidence_ids" },
      "on_fail": { "reason": "claim evidence ids are optional; cited ids, if any, must be real", "hints": ["omit evidence_ids unless a real ledger id is explicitly available"] }
    },
    {
      "op": "for_all",
      "over": "findings",
      "require": { "pred": "non_empty", "field": "evidence_refs" },
      "on_fail": { "reason": "finding evidence ids are optional; cited ids, if any, must be real", "hints": ["omit evidence_refs unless a real ledger id is explicitly available"] }
    },
    {
      "op": "for_all",
      "over": "coverage",
      "where": { "pred": "eq", "field": "status", "value": "found" },
      "require": { "pred": "non_empty", "field": "evidence_refs" },
      "on_fail": {
        "reason": "coverage evidence ids are optional; found cells are resolved from DB/ledger truth",
        "hints": ["omit evidence_refs unless a real ledger id is explicitly available"]
      }
    },
    {
      "op": "for_all",
      "over": "coverage",
      "where": { "pred": "eq", "field": "status", "value": "checked_empty" },
      "require": { "pred": "non_empty", "field": "evidence_refs" },
      "on_fail": {
        "reason": "coverage evidence ids are optional; checked_empty is resolved from DB/ledger truth or terminal coverage",
        "hints": ["omit evidence_refs unless a real ledger id is explicitly available"]
      }
    },
    { "op": "named_check", "check": "surface_coverage" },
    {
      "op": "coverage_complete",
      "derive_from_evidence": true,
      "error_is_terminal": false,
      "require_note_for_other": true,
      "authoritative_found": true,
      "authoritative_techniques": ["GOLISH-EAS-LIVENESS", "GOLISH-EAS-PORT", "GOLISH-EAS-SERVICE-FINGERPRINT", "GOLISH-EAS-WEB-FINGERPRINT"],
      "$comment_derive": "Found cells require guarded current-run DB truth: domain/URL LIVENESS comes from HTTP observations, IP/CIDR LIVENESS/PORT from their own active wrapper outcome, concrete-IP SERVICE from every confirmed open port, and exact-origin WEB from WhatWeb outcomes. real_ip and dns_records are never liveness, authorization or coverage projection sources. Hand-written found cells do not count.",
      "on_fail": {
        "reason": "external attack surface incomplete: some in-scope assets were not fully mapped (liveness / port / service-fingerprint / web-fingerprint)",
        "hints": [
          "for every applicable cell, reach found / checked_empty / blocked|not_applicable+note. Explicit IP runs PORT then SERVICE per confirmed open port. CIDR runs only range LIVENESS/PORT; child IPs created by guarded in-range landing receive SERVICE/WEB in a supplemental wave. Domain/URL uses exact Host/SNI LIVENESS and WEB per confirmed origin. Wildcard pattern rows have no executable cells",
          "intel-source data (FOFA/Shodan/...) is a lead, not proof. Confirm through the four eas_* wrappers; their hidden backend recipes use port, service, HTTP and WhatWeb engines and land guarded DB truth. Never call raw scanners or pentest_run. JS/API extraction happens in Enumeration"
        ]
      }
    },
    {
      "op": "coverage_denominator",
      "authoritative": true,
      "min_sample_ratio_pct": 100,
      "$comment_authoritative": "EAS found coverage is now adjudicated from DB/ledger truth (targets.ports + fingerprints + technique_outcomes), so denominator fields should not be hand-copied into the deliverable. coverage_complete owns completeness; denominator remains as a documented no-op for this slim DB-truth stage.",
      "on_fail": {
        "reason": "external attack surface denominator incomplete",
        "hints": [
          "for service fingerprint coverage set tested_units to the number of discovered open ports fingerprinted and total_units to the number of discovered open ports",
          "if a host has no open ports, mark SERVICE-FINGERPRINT not_applicable with a note instead of checked_empty total_units=0",
          "do not treat HTTP liveness alone as PORT or SERVICE-FINGERPRINT coverage"
        ]
      }
    }
  ],

  "$comment_expected_techniques": "The four IDs are the global EAS vocabulary; technique_resolver applies them by identity. domain/url=LIVENESS plus WEB once an exact origin is confirmed; ip=LIVENESS+PORT+SERVICE plus WEB when confirmed; cidr=LIVENESS+PORT only on the range row; wildcard=all N/A. Exact-origin completeness is a separate authoritative barrier. JS/API belongs to Enumeration.",
  "expected_techniques": ["GOLISH-EAS-LIVENESS", "GOLISH-EAS-PORT", "GOLISH-EAS-SERVICE-FINGERPRINT", "GOLISH-EAS-WEB-FINGERPRINT"],

  "$comment_stage_run": "stage_run fan-out (design 2026-06-13-stage-run-fanout \u00a73.2): specialist = the per-org sub-agent the stage_run tool fans out (EAS \u2192 prober, the ACTIVE surface-mapper split from Pentester, mirroring recon for target_intel); coverage_axis = the human-readable technique columns StageRunView renders per org row (one per expected technique: liveness / port / service-fingerprint / web-fingerprint).",
  "specialist": "prober",
  "coverage_axis": ["LIVENESS", "PORT", "SERVICE", "WEB"],

  "$comment_min_invocations": "2026-06-25 EAS slim DB-truth closeout: no hard tool floor in the deliverable. Active work is proven by coverage_complete reading DB/ledger facts (targets liveness/ports, fingerprints, technique_outcomes). A required_checks_done/http_probe floor only reintroduces model hand-copying and blocks otherwise valid DB-truth runs.",
  "min_invocations": {},

  "$comment_facts_from_db_truth": "2026-07-06: EAS now matches target_intel's slim deliverable pattern for found cells. The worker runs active tools and lets DB/ledger truth fill found LIVENESS/PORT/SERVICE-FINGERPRINT/WEB-FINGERPRINT cells; submit only negative/blocked terminal cells the DB cannot derive.",
  "facts_from_db_truth": true,

  "max_other_skips": 2,

  "human_approval": {
    "required_before": ["active_scan", "exploit_validation"]
  },

  "agent_continuity": "single_session",

  "inherits_evidence_from": [
    {
      "stage_kind": "target_intel",
      "evidence_kinds": ["dns_a", "asn", "whois", "subdomain"]
    }
  ]
}
