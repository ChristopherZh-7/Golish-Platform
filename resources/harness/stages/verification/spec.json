{
  "$schema": "../../../../docs/design/2026-05-26-stage-harness-mvp-external-attack-surface.md#4.1",
  "$comment": "Stage spec: verification. L4 controlled exploit validation. Approval-gated. Phase A.",

  "id": "verification",
  "kind": "verification",
  "risk_level": "critical",

  "requires_stages": ["vuln_triage"],
  "allowed_next_stages": ["access_validation", "reporting"],

  "allowed_tool_types": ["web/injection", "exploit"],

  "deliverable_schema": "StageDeliverable",
  "gate_validator": "validate_stage_gate",

  "min_invocations": {},

  "max_other_skips": 1,

  "human_approval": {
    "required_before": ["exploit_validation"]
  },

  "agent_continuity": "single_session",

  "inherits_evidence_from": [
    {
      "stage_kind": "vuln_triage",
      "evidence_kinds": ["vuln_finding"]
    }
  ],

  "$comment_p2": "P2 verification-gate SAMPLE — tune to taste. Evidence ids are optional model fields; verification evidence must be resolved from backend ledger/DB truth rather than requiring the model to hand-copy ids.",
  "required_evidence_kinds": ["exploit_verified"],
  "finding_verification": {
    "min_severity": "high",
    "require_evidence_kinds": ["poc", "exploit_verified"]
  },

  "$comment_gate_rules": "DSL sample (design 2026-06-05). Declarative twin of finding_verification: every high+ finding must carry at least one evidence ref. Add more rules here to extend pass-criteria with zero Rust.",
  "gate_rules": [
    {
      "op": "for_all",
      "over": "claims",
      "require": { "pred": "non_empty", "field": "evidence_ids" },
      "on_fail": { "reason": "claim evidence ids are optional; cited ids, if any, must be real", "hints": ["omit evidence_ids unless a real ledger id is explicitly available"] }
    },
    {
      "op": "for_all",
      "over": "findings",
      "require": { "pred": "non_empty", "field": "evidence_refs" },
      "on_fail": { "reason": "finding evidence ids are optional; cited ids, if any, must be real", "hints": ["omit evidence_refs unless a real ledger id is explicitly available"] }
    },
    {
      "op": "for_all",
      "over": "findings",
      "where": { "pred": "severity_at_least", "min": "high" },
      "require": { "pred": "non_empty", "field": "evidence_refs" },
      "on_fail": { "reason": "verification finding evidence ids are optional; backend resolves proof from DB/ledger truth" }
    },
    {
      "op": "candidate_disposition_complete",
      "require_evidence_for_verified": true,
      "on_fail": { "reason": "every approved candidate must reach a terminal disposition (verified/refuted/blocked)", "hints": ["verified/refuted evidence is resolved from DB/ledger truth; do not hand-copy ids", "blocked \u2192 note the WAF/authz reason"] }
    }
  ]
}
