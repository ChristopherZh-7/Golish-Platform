// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { InstallInfo } from "./InstallInfo";
import type { InstalledVia } from "./InstalledVia";
import type { OutputConfig } from "./OutputConfig";
import type { ToolParam } from "./ToolParam";
import type { ToolSkill } from "./ToolSkill";

/**
 * Configuration for a single penetration testing tool, parsed from JSON config files.
 *
 * Mirrored to the frontend via ts-rs. The `asset_intel` / `asset_intel_providers`
 * / `integration` fields are `#[ts(skip)]`'d: they carry deep backend-only
 * descriptors the UI never reads, so excluding them keeps the generated
 * `ToolConfig` lean (and the heavy `AssetIntelToolConfig` tree out of the
 * bindings). The frontend view-model layers its runtime-only
 * `executableReady` / `executableError` on top via intersection.
 */
export type ToolConfig = { id: string, name: string, description: string, 
/**
 * Path to the executable relative to the tools directory.
 */
executable: string, 
/**
 * Runtime environment: "native", "java8", "javaLatest", "python3.11", "pip3.11",
 * "nodeLatest", etc.
 */
runtime: string, 
/**
 * Launch mode: "cli", "gui", "web", "vbs", "normal", "directory".
 */
launchMode: string, args: string, version: string, tags: Array<string>, category: string, subcategory: string, 
/**
 * Pentest 7-phase tag(s) for this tool. Multi-phase tools (e.g. amass spans
 * recon + enum) list every phase they fit. Empty = unclassified; defaults
 * in unchanged on load so legacy configs without this field keep working.
 * Valid values are enforced by `validate()` (see `VALID_PENTEST_PHASES`).
 */
pentestPhase: Array<string>, jvm_options: Array<string>, 
/**
 * Tool icon (emoji or image path).
 */
icon: string, 
/**
 * Runtime version requirement (e.g. "3.11", "21").
 */
runtimeVersion: string, 
/**
 * Installation method and source.
 */
install: InstallInfo | null, 
/**
 * Reusable skill definitions (quick-action presets).
 */
skills: Array<ToolSkill>, 
/**
 * CLI argument definitions for UIs.
 */
params?: Array<ToolParam>, 
/**
 * Output parsing configuration for automatic data extraction.
 */
output: OutputConfig | null, 
/**
 * Tool importance: "essential" (core workflow), "recommended", or "optional".
 */
tier: string, 
/**
 * Whether the tool's executable is present on disk (populated by scanner).
 */
installed: boolean, 
/**
 * Whether the required runtime environment is available (populated by scanner).
 */
envReady: boolean, 
/**
 * Where the tool was found, if installed (populated by scanner).
 * Used by the Tool Manager to render different UI per provenance and to
 * dispatch the correct uninstall strategy.
 */
installedVia?: InstalledVia | null, };
