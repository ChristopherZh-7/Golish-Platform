---
description: Golish-Platform 架构独立评估报告（B+ 现状 · 五大致命问题 · 改造优先级）
globs:
alwaysApply: false
---

# Golish-Platform 架构评估（独立视角 · 2026-05-02）

> 本评估由独立架构师审阅完成，**未参考**项目此前存在的 refactor-roadmap / architecture-upgrade-plan。
> 综合评级：**B+（良好但未到优秀）** — 技术品位 A · 纪律性 B · 多处"开工没封顶"

---

## 1. 项目档案（事实层）

| 维度 | 数据 |
|---|---|
| 类型 | Tauri 2 桌面 Agentic App（Cursor/Claude Code 类 + pentest + vuln-intel）|
| 后端 | 45 crate · 1,066 文件 · **177K Rust LOC** |
| 前端 | **125K TS/TSX LOC** · 64 组件目录 · 33 hooks · 73 lib · 14 Zustand slice |
| IPC 面 | **548 个 `#[tauri::command]`** |
| 技术栈 | Rust 2021 + Tauri 2 / React 19 + TS 6 / Zustand+Immer / Tailwind 4 |
| LLM | `rig-core` 0.36 + 自维护 4 个 provider fork |
| 可观测性 | OTel + Langfuse（semantic convention 级别）|

---

## 2. 优势（列入简历的部分）

1. **后端 6 层 DAG 真实可验证**：L1 基础（`core/settings/context/models/mcp`）→ L2 领域（`events/session/llm-providers/tools`）→ L3 AI 原语 → L4a/b/c agent 三段 → L5 umbrella → L6 Tauri app。
2. **业务域六边形架构落地**：`pentest` / `pentest-domain` / `pentest-mcp` 三段分离；`vuln-intel` 同理。
3. **Zustand 切片化教科书级**：14 个 slice + selectors/effects/actions/public-api 分离，单元 + 性能测试齐全。
4. **可观测性生产级**：三层 span（`chat_message → agent → llm_completion`）+ `gen_ai.*` 与 `langfuse.observation.*` 双重兼容。
5. **测试基建扎实**：20 个 Playwright e2e spec + crate 级 `tests/api_contract.rs`。

---

## 3. 五大致命问题（优先处理）

### 🔴 问题 1 — `golish-ai` umbrella crate 是纯债务
`backend/crates/golish-ai/src/lib.rs` 全部是 `pub use` 的 re-export，存在理由仅"向后兼容"。每次 agent 层变动要维护 4 个 `pub use` 列表。**典型"重构半成品"症状**。

### 🔴 问题 2 — `agent-loop` vs `agentic-loop` 命名灾难
两个 crate 名仅一字之差。职责混淆：`agent-loop` 是基础件（tool executors/HITL/planner），`agentic-loop` 是循环体（stream/eval/mocks）。对新贡献者不友好。

### 🔴 问题 3 — 反向依赖 `golish-prompts → golish-sub-agents`
L3 的 prompts 原语层依赖了同层或上层的 sub-agents，破坏 DAG 单向性。

### 🔴 问题 4 — `run_agentic_loop_unified` 是 300 行上帝函数
`backend/crates/golish-agentic-loop/src/agentic_loop/mod.rs` 一个函数做 12 件事（span/budget/compaction/hooks/stream/reflector/dispatch…）。每步都 delegate 到子模块了，**但编排顺序本身固化在 mod.rs**，无法独立单测。

### 🔴 问题 5 — `frontend/mocks.ts` = 4,213 行 / 136 KB 单文件
比整个 Zustand store 还大。混入 Tauri invoke stub + AI 事件流 + session/timeline/pentest fixtures。Biome/Vite HMR 慢、PR diff 滚几百屏、并发改必冲突。

### 🟡 问题 6 — 单体 `AppState` 仍在扩张
`backend/crates/golish/src/state/mod.rs` 的 `AppState` 有 15 个字段。新子状态（`DbState` / `PtyState` / `McpManaged` / …）已定义但全部 `#[allow(unused_imports)]`——消费者没迁。

### 🟡 问题 7 — 548 Tauri commands 没有"路由层"
全部塞进一个 `tauri::generate_handler![]`，靠 `include!` 绕过宏 hygiene 限制（代码注释自己承认是 hack）。前端需逐个知道所有命令名，无 namespace。

### 🟡 问题 8 — `docs/architecture.md` 严重过期
官方文档说 3 层结构，实际 6 层 + 45 crate。`golish-ai` 说是"agent orchestration"实则空 umbrella。**对新贡献者有误导性**。

### 🟢 问题 9 — 45 crate 的认知成本偏高
个别拆得过细：`golish-json-repair` / `golish-udiff`（若仅一个 consumer，心智成本大于收益）。

---

## 4. 综合评级表

| 维度 | 评级 | 备注 |
|---|---|---|
| 分层 DAG 清晰度 | **A-** | 边界清楚，仅一处反向依赖 |
| 职责隔离 (SRP) | **B** | agent loop + AppState 上帝对象 |
| 命名一致性 | **C+** | agent-loop vs agentic-loop 败笔 |
| 测试覆盖 | **A-** | e2e + 单元 + 集成齐全 |
| 可观测性 | **A** | Langfuse+OTel 生产级 |
| 前端架构 | **B+** | slice 化好，mocks.ts 黑点 |
| 文档同步 | **C** | architecture.md 严重过期 |
| 演进节奏 | **B** | 多处"半拆完"残留 |
| IPC 攻击面 | **B-** | 548 端点需系统性 hardening |

**综合**：**B+（良好但未到优秀）**

**一句话画像**：*"技术品位 A、纪律性 B 的项目。问题不在'没架构'，而在'拆了不敢收，改了不敢删'"*。

---

## 5. 架构师决策（已拍板）

| 决策点 | 方案 | 理由 |
|---|---|---|
| agent 层改名 | `golish-agent-kit` + `golish-agent-runtime` | 口语化，职责一眼分辨 |
| `golish-ai` 处理 | **彻底删除 crate** | 拖着反而乱；向后兼容交给 `CHANGELOG` |
| `mocks.ts` 迁移 | **`git mv` 直接拆** | 历史追得到，shim 制造新债务 |
