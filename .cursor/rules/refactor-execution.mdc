---
description: Golish B+→S+ 架构改造执行计划（两 session 并行 · checkpoint · 勾选协议）
globs:
alwaysApply: false
---

# Golish-Platform 架构改造执行计划

> 配套文档：`architecture-evaluation.mdc`（评估结论） + 本文件（执行）
> 起点：B+ · 终点：S+
> 策略：**Session A（后端 agent 层）** 与 **Session B（前端+文档+CI）** 并行，冲突域几乎为零

---

## 🚨 Resume Instructions — 给接手 Session 看

> **如果上一个 session 中断了，新 session 来接手**：不要重新评估，按这里的指引继续。

### 第一步：核对当前进度（30 秒）

在本文件底部的"**完成度仪表盘**"看已做了什么。目前状态：

| 任务 | 状态 |
|---|---|
| A1 修反向依赖 | ✅ Done |
| A2 agent 层改名 | ✅ Done |
| A3 杀 umbrella | ✅ Done |
| A4 AppState 迁移 | ✅ Done |
| B1 拆 mocks.ts | ❌ 用户明确跳过 |
| B2 CI 守门 | ✅ Done |
| B3 重写架构文档 | ✅ Done |
| B4 Tauri ADR | ✅ Done |
| **C1 TurnExecutor** | **🟡 6/9 · C1-0~C1-5 落地 (ADR + PreFlight + Compaction + FirstIterHooks/TokenEstimate + Completion + AssistantPush/ReflectorOrBreak/ToolDispatch)，C1-6 起继续** |

### 第二步：验证前面工作没崩（60 秒）

```bash
cd backend
cargo check --workspace --tests  # 必须 0 errors / 0 warnings
python3 ../scripts/check_dag.py  # 必须 "DAG clean across 43 crates"
bash ../scripts/check_file_sizes.sh  # 必须 "all files within size budget"
```

**任何一条红** → 先定位是否上一个 session 留下了脏工作树；
`git status` + `git diff` 审查未提交改动；必要时先 `git stash`。

### 第三步：继续 C1 迁移（主任务）

按 `docs/adr/0010-turn-executor-state-machine.md` 的迁移计划：

| PR | 工作 | 下一个做 |
|---|---|---|
| ~~C1-4~~ | ~~抽 `StreamStart` + `StreamProcess` phase~~ | ✅ Done |
| ~~C1-5~~ | ~~抽 `AssistantPush` + `ReflectorOrBreak` + `ToolDispatch`~~ | ✅ Done |
| **C1-6** | 主 `loop` 替换为 phase 调度器，`mod.rs` ≤ 150 行 | ← 先做这个 |
| C1-7 | `TurnInterceptor` trait · Langfuse/HITL 搬出 |  |
| C1-8 | 每 phase 补 1+ 单元测试 |  |

每个 PR 的标准流程：
1. 照 `C1-1` 的模式（见 `turn/phases/pre_flight.rs` 模板）在 `turn/phases/` 下新建 phase 文件
2. 把 `agentic_loop/mod.rs` 里对应的内联代码段**删掉**，换成 `phase::run(&mut turn_state, ctx, ...)` 调用
3. 如果 phase 需要额外的 `turn_state` 字段，在 `turn/state.rs` 的 `TurnState` 里加新字段（增长是预期的）
4. `cargo check --workspace --tests` 必须绿
5. 更新本文件里 `C1` 任务的"已完成"列表 + `+1` 进度计数

### 第四步：重要指引 / 常见陷阱

1. **不要调用任何 subagent**（铁律一，下文详述）
2. **不要动已勾 `[x]` 的任务**（铁律三）
3. **PoC 的 `TurnState` 当前仅 `iteration` 字段**——其他 local vars 在 mod.rs 里还是 local，每个 phase PR 可能需要把更多 vars 收进 TurnState。**这是预期的增量扩展，不是架构债**。
4. **`pre_flight::run` 改变了一个行为**：cancel/budget 检查现在先于 `pre_turn_compaction`。这是改进。**保持这个顺序**。
5. **B1（拆 `frontend/mocks.ts`）用户明确跳过**，不要主动做。
6. **AppState 里的字段保留**——子状态从它 extract 出 Arc 共享，不要尝试删 AppState 字段（会牵动 `AppState::new` + `bootstrap.rs`）。

### 第五步：交接 commit 边界

上一个 session 在 3 个 commit 里封存了所有成果（见 `git log --oneline -10`）：
1. `chore(rules): architecture evaluation + refactor execution roadmap`
2. `refactor(agents): stack cleanup (A1-A4) + TurnExecutor PoC (C1-0/C1-1)`
3. `ci+docs: architecture guards + refreshed docs + ADRs (B2/B3/B4 + ADR-0010)`

**继续工作**请在这 3 个 commit 之后叠加新 commit（每个 C1-N 一个 commit）。推荐前缀：`refactor(agent-runtime): C1-N <phase-name> extracted`。

---

## ⚠️ 四条铁律（违反任何一条 = 任务失败）

### 铁律一：**绝对禁止使用任何 subagent / Task tool**
- 不允许调用 `Task` 工具开 generalPurpose / explore / shell / cursor-guide / best-of-n-runner 任何子代理
- 必须由当前 session 直接执行所有读、写、搜索、构建、测试操作
- 允许工具：`Read` / `Write` / `StrReplace` / `Glob` / `Grep` / `Shell` / `ReadLints` / `Delete`

### 铁律二：**执行前必须先 Checkpoint**
任何任务项开工前，**必须先跑该项提供的「检查命令」核实当前进度**。严禁凭印象判定。

### 铁律三：**已勾选 → 跳过；未勾选 → 检查后再勾上**
- `[x]` = 已完成 → 跳过
- `[ ]` = 未完成 → 先跑检查命令，若实际已完成 → 用 `StrReplace` 改为 `[x]` 并附证据

### 铁律四：**绿构建 + 小步提交**
- 每个 PR ≤ **500 行 diff**，超过必须拆
- 每次提交前：`cargo check --workspace` + `cd frontend && npx tsc --noEmit` 必须 **0 errors / 0 warnings**
- 新拆 trait 必须给出 `MockX` 或 `NullX`

---

## Checkpoint 操作流程（每个任务项的标准动作）

```
1. 读取本文件，定位目标任务项编号（如 A1）
2. 看复选框状态：
   - [x] → STOP，跳到下一项
   - [ ] → 继续步骤 3
3. 跑该任务的「检查命令」（下文每项都给）
4. 判定：
   - 实际已完成 → 用 StrReplace 把 [ ] 改为 [x]，追加检测证据
   - 确实未完成 → 进入「执行步骤」，完成后改 [x]
5. 移到下一项
```

---

## Session 分工

| | Session A（后端 agent 层）| Session B（前端+文档+CI） |
|---|---|---|
| 文件域 | `backend/crates/golish-{ai,agent-loop,agentic-loop,agent-bridge,prompts,sub-agents}/` + `backend/crates/golish/src/state/` | `frontend/`、`docs/`、`.github/workflows/` |
| 绝不触碰 | `frontend/`、`docs/`、`.github/` | `backend/crates/golish-*/src/` |
| 可读只读 | workspace `Cargo.toml`（只看不改） | workspace `Cargo.toml`（只看不改） |

**协同点**：
- Session A 的 **A2（改名）** 完成后通知 Session B → B3 才能动 `docs/architecture.md`
- 所有 A/B 做完后 → 合并做深度重构 **C1**

---

## Phase 1: 手术式整理（2 session · 1 周 · 8 个 PR）

### Session A 流水线（后端 agent 层）

---

#### [x] A1 · 修反向依赖 `golish-prompts → golish-sub-agents` — ✅ Done 2026-05-02

> ✅ **执行证据**：
> - `grep "^golish-sub-agents" backend/crates/golish-prompts/Cargo.toml` = 空
> - `grep -rn "use golish_sub_agents" backend/crates/golish-prompts/src/` = 空（只剩文档注释）
> - `cargo check --workspace --tests` 全绿（1m 21s）· 0 errors / 0 warnings
>
> **实际迁移路径**：
> 1. `git mv backend/crates/golish-prompts/src/contributors/sub_agents.rs → backend/crates/golish-sub-agents/src/prompt_contributor.rs`
> 2. `golish-sub-agents/src/lib.rs` 新增 `pub mod prompt_contributor;` + `pub use prompt_contributor::SubAgentPromptContributor;`
> 3. `golish-prompts/src/contributors/mod.rs` 清空 sub_agents 子模块 + 删除 `create_default_contributors` 函数
> 4. `golish-prompts/src/lib.rs` 删除 `SubAgentPromptContributor` re-export
> 5. `golish-prompts/Cargo.toml` 删除 `golish-sub-agents = { workspace = true }` 依赖
> 6. 新建 `backend/crates/golish-agent-bridge/src/contributors.rs`，把 `create_default_contributors` 函数迁过来（bridge 本就依赖 prompts + sub-agents）
> 7. `golish-agent-bridge/src/lib.rs` 新增 `pub mod contributors;`、移除 `pub use golish_prompts::contributors`（避免与本 crate `contributors` 冲突）
> 8. `golish-evals/Cargo.toml` 新增 `golish-agent-bridge = { workspace = true }` 依赖
> 9. `golish-evals/src/executor/mod.rs` 改 import 到 `golish_agent_bridge::contributors::create_default_contributors`
>
> **架构效果**：
> - `golish-prompts` 现在 L3 真正"纯净"（只依赖 `golish-core` + `golish-llm-providers`）
> - `SubAgentPromptContributor` 与 `SubAgentRegistry` 在同一 crate，封闭性更好
> - `create_default_contributors` 迁到 bridge 层，符合"组合发生在装配层"原则

**问题**：
`backend/crates/golish-prompts/Cargo.toml` 依赖 `golish-sub-agents`，破坏 L3 原语层 DAG 单向性。

**检查命令**：
```bash
grep "^golish-sub-agents" backend/crates/golish-prompts/Cargo.toml
```
有输出 = 未完成；无输出 = 已完成。

**执行步骤**：
1. `grep -r "use golish_sub_agents" backend/crates/golish-prompts/src/` 列出所有使用点
2. 把"与 sub-agent 相关的 prompt 构造逻辑"从 `golish-prompts/src/` 移到 `golish-sub-agents/src/prompts/` 新模块
3. 反过来：`golish-sub-agents/Cargo.toml` 新增 `golish-prompts` 依赖（这才是正向）
4. 删除 `golish-prompts/Cargo.toml` 的 `golish-sub-agents` 依赖
5. 全仓搜索调用点，改 import 路径

**完成判据**：
- `grep "^golish-sub-agents" backend/crates/golish-prompts/Cargo.toml` = 空
- `cargo check -p golish-prompts -p golish-sub-agents -p golish-agent-loop` 绿
- `cargo check --workspace` 绿

**预计**：2-3h / ~150 行 diff / 1 PR

---

#### [x] A2 · 重命名 `agent-loop` → `agent-kit`，`agentic-loop` → `agent-runtime` — ✅ Done 2026-05-02

> ✅ **执行证据**：
> - `ls backend/crates/ | grep -E "^golish-(agent-loop|agentic-loop)$"` = 空
> - `grep -rn "golish[_-]agent\(ic\)\?[_-]loop" backend/crates/ backend/Cargo.toml` = 空
> - `cargo check --workspace --tests` 全绿（首跑 2m05s、增量 17s）· 0 errors / 0 warnings
>
> **实际改动**（两 PR 合并进 A2）：
> 1. `git mv backend/crates/golish-agent-loop     → backend/crates/golish-agent-kit`
> 2. `git mv backend/crates/golish-agentic-loop   → backend/crates/golish-agent-runtime`
> 3. 两个子 Cargo.toml 的 `[package].name` 更新
> 4. workspace 根 `backend/Cargo.toml`：`members` / `default-members` / `[workspace.dependencies]` 三处同步
> 5. 全仓 sed（限 `.rs` + `.toml`，排除 `target/` 与 `docs/`）：
>    - **先** `golish-agentic-loop → golish-agent-runtime`（长串优先，避免被短串 sed 误伤）
>    - **再** `golish-agent-loop → golish-agent-kit`
>    - 同样顺序处理下划线版本：`golish_agentic_loop` / `golish_agent_loop`
> 6. 修复两个 lib.rs 顶部文档注释：sed 后 ASCII 框图因字长变化错位，顺手重写为更简洁的层级说明
>
> **命名决策理由**：
> - `agent-kit` = 底层"工具箱"（tool executors / HITL / planner / db tracking）
> - `agent-runtime` = 顶层"运行时"（streaming loop / eval / mocks）
> - 两个词**不再共享任何字符串**，5 秒内可分辨职责

**问题**：
两 crate 名仅一字之差（`agent-loop` vs `agentic-loop`），职责区分不清晰。

**决策**：
- `golish-agent-loop` → **`golish-agent-kit`**（底层设施：tool executors / HITL / planner / db tracking）
- `golish-agentic-loop` → **`golish-agent-runtime`**（顶层编排：agentic_loop/ + eval + mocks）

**检查命令**：
```bash
# 若输出包含 agent-loop 或 agentic-loop 的目录则未完成
ls backend/crates/ | grep -E "^golish-(agent-loop|agentic-loop)$"
```

**执行步骤**：
1. `git mv backend/crates/golish-agent-loop backend/crates/golish-agent-kit`
2. `git mv backend/crates/golish-agentic-loop backend/crates/golish-agent-runtime`
3. 改两个 `Cargo.toml` 的 `[package].name`
4. 改 workspace 根 `Cargo.toml`：members + default-members + `[workspace.dependencies]` 三处
5. 全仓 sed：
   ```bash
   # 先改 package name（连字符版本）
   grep -rl "golish-agent-loop" backend/ | xargs sed -i '' 's/golish-agent-loop/golish-agent-kit/g'
   grep -rl "golish-agentic-loop" backend/ | xargs sed -i '' 's/golish-agentic-loop/golish-agent-runtime/g'
   # 再改 crate name（下划线版本）
   grep -rl "golish_agent_loop" backend/ | xargs sed -i '' 's/golish_agent_loop/golish_agent_kit/g'
   grep -rl "golish_agentic_loop" backend/ | xargs sed -i '' 's/golish_agentic_loop/golish_agent_runtime/g'
   ```
6. **⚠️ 注意：CHANGELOG / docs 里的历史记录保留不变**

**完成判据**：
- `ls backend/crates/ | grep -E "golish-(agent-loop|agentic-loop)"` = 空
- `grep -rn "golish[_-]agent\(ic\)\?-loop" backend/ --include="*.rs" --include="*.toml"` = 空
- `cargo check --workspace` 绿
- `cargo test --workspace` 绿（核心 agent crate 至少）

**预计**：3-4h / ~600 行 diff（机械替换，单独 1 个大 PR）

---

#### [x] A3 · 杀 `golish-ai` umbrella crate — ✅ Done 2026-05-02

> ✅ **执行证据**：
> - `ls backend/crates/golish-ai` = 不存在
> - `grep "golish-ai\b" backend/Cargo.toml` = 空
> - `grep -rn "use golish_ai\b" backend/crates/` = 空（所有 `use` 语句已切换到具体 crate）
> - `cargo check --workspace --tests` 全绿（42.94s）· 0 errors / 0 warnings
>
> **实际改动**（一次性提交）：
> 1. **Perl 8 组正则批量替换**（31 个文件）：按 symbol 精确映射到 `golish_agent_kit` / `golish_agent_runtime` / `golish_agent_bridge` / `golish_prompts` / `golish_events`。注意 `task_orchestrator::bridge_executor` 要在通用 `task_orchestrator` 之前替换（避免被后者覆盖）
> 2. **手工修花括号内混合 import**：`golish/src/ai/commands/core/chat.rs:115` 的 `use golish_ai::task_orchestrator::{bridge_executor::X, TaskOrchestrator}` 拆成两行（bridge_executor 来自 agent-bridge，TaskOrchestrator 来自 agent-kit）
> 3. **重写 `golish/src/ai/mod.rs`**：把 `pub use golish_ai::*;` glob 替换成和原 umbrella 等价的 5 组显式 `pub use`，同时就地提供 `task_orchestrator` 复合 mod（bridge_executor + agent_kit）
> 4. **tracing directive 修复**（bug fix）：原本 `"golish_ai=error"` 在 crate 删除后会变成死指令，展开为 4 个新 crate 的 directives（`golish_agent_kit` / `_runtime` / `_bridge` / `_prompts`），影响 `bootstrap/mod.rs` + 3 个 eval 入口文件
> 5. `git rm -r backend/crates/golish-ai`（crate 目录删除）
> 6. workspace 根 `backend/Cargo.toml`：`members` / `default-members` / `[workspace.dependencies]` 三处移除 `golish-ai`
> 7. 消费者 `Cargo.toml` 依赖改写：
>    - `golish/Cargo.toml`：`golish-ai` → `agent-kit` + `agent-runtime` + `agent-bridge` + `prompts`
>    - `golish-evals/Cargo.toml`：`golish-ai` → `agent-kit` + `agent-runtime` + `agent-bridge` + `prompts`
>    - `golish-swebench/Cargo.toml`：`golish-ai` → `agent-runtime`（只用到 `agentic_loop` 和 `eval_support`）
> 8. `CHANGELOG.md` Unreleased 段落添加 "BREAKING CHANGES — Architecture" 章节，列出完整 symbol 迁移表
>
> **架构效果**：
> - Agent stack 现在是**清晰的三段**：`agent-kit`（L4a 建筑块）→ `agent-runtime`（L4b 循环体）→ `agent-bridge`（L4c 装配点），再无 umbrella 遮盖
> - `golish` Tauri crate 直接依赖 4 个具体 crate，依赖意图一目了然
> - 45 crate → **44 crate**（umbrella 正式下架）
>
> **残留说明**：
> - 50+ 处 `golish-ai` 字符串仅出现在**文档注释和 CHANGELOG 历史**里（"extracted from golish-ai"、"decouples golish-ai from X" 等描述性文字），不影响编译和运行
> - 这些注释属于"低优先级遗产"，将来顺手更新即可，本 A3 不追求完美清理

**前置**：必须在 A2 完成后做

**问题**：
`golish-ai` 是纯 re-export 的空 umbrella，backward compatibility 债务。

**决策**：**彻底删除 crate**，所有消费者直接 import 具体 crate。

**检查命令**：
```bash
ls backend/crates/golish-ai 2>/dev/null && echo "STILL_EXISTS"
```
有 `STILL_EXISTS` 输出 = 未完成。

**执行步骤**：
1. 列出所有下游消费者：
   ```bash
   grep -rln "golish_ai\|golish-ai" backend/ frontend/ docs/ --include="*.rs" --include="*.toml" --include="*.md"
   ```
2. 按出现位置分三类处理：
   - **Rust `use golish_ai::agentic_loop::*`** → `use golish_agent_runtime::agentic_loop::*`
   - **Rust `use golish_ai::task_orchestrator::*`** → `use golish_agent_bridge::bridge_executor; use golish_agent_kit::task_orchestrator::*`
   - **Cargo.toml `golish-ai = ...`** → 按实际用到的 crate 替换（可能是 kit + runtime + bridge）
3. 删除 `backend/crates/golish-ai/` 整个目录
4. workspace 根 `Cargo.toml`：从 `members` / `default-members` / `[workspace.dependencies]` 三处移除 `golish-ai`
5. 更新 `CHANGELOG.md` 添加 `BREAKING: removed golish-ai umbrella; import specific crates`

**完成判据**：
- `ls backend/crates/golish-ai 2>/dev/null` = 空
- `grep -rn "golish_ai\|golish-ai" backend/ --include="*.rs" --include="*.toml"` = 空（只允许 CHANGELOG）
- `cargo check --workspace` 绿
- `cargo test --workspace` 绿

**预计**：4-6h / ~400 行 diff / 1-2 PR

---

#### [x] A4 · `AppState` 迁移收尾 — ✅ Done 2026-05-02

> ✅ **执行证据**：
> - `grep -c "allow(unused_imports)" backend/crates/golish/src/state/mod.rs` = **0**
> - 子状态文件 `#[allow(dead_code)]` struct 属性 = **0**（db/mcp/pty/sidecar/telemetry 全部清零）
> - `cargo check --workspace --tests` 全绿 · 50s · 0 errors / 0 warnings
>
> **已迁移子状态（5 个）**：
>
> | 子状态 | 迁移策略 | 新命令数 |
> |---|---|---|
> | `TelemetryState` | `settings/commands.rs` 2 条整迁 | 2 |
> | `McpManaged` | 选迁 `mcp_list_servers` + `mcp_list_tools`，跨域 connect/disconnect 保留 AppState | 2 |
> | `PtyState` | `commands/proc/pty.rs` 7 条整迁（字段名适配：`manager` / `output_tap` / `active_session`） | 7 |
> | `SidecarManaged` | `sidecar/commands/` 30 条迁 26，**重命名字段 `config`→`sidecar_config` / `state`→`sidecar_state` 零改命令体**，跨域 4 条保留 AppState | 26 |
> | `DbState` | `tools/sensitive_scan.rs` 5 条 + `tools/pentest/zap/*` 多条（用双 State 模式：业务 PentestState + 数据 DbState），新增 `pool_arc()` getter | 10+ |
> | `PentestToolState` | **直接删除**（冗余 wrapper，`Arc<ConfigManager>` 早已作为独立 State `.manage()`） | N/A |
>
> **实际改动总结**：
> 1. 6 个 `state/*.rs` 文件清理（去 `#[allow(dead_code)]`，加 `from_shared()` constructor）
> 2. `state/pentest.rs` 删除（冗余）
> 3. `state/mod.rs` 添加 5 个 `extract_*` 方法 + 清零所有 `#[allow(unused_imports)]`
> 4. `app/tauri_app.rs` 新增 4 个 `.manage()`（`telemetry_state` / `mcp_managed` / `pty_state` / `sidecar_managed`）
> 5. `state/db.rs` 新增 `pool_arc()` getter（零成本 Arc clone）
> 6. Sidecar 字段重命名：`config → sidecar_config`, `state → sidecar_state`（匹配 AppState 字段名，让 sed 只需改签名不改命令体）
> 7. 批量 sed：`sensitive_scan.rs` 5 条、`pentest/zap/*.rs` 多条、`sidecar/commands/*.rs` 全量
> 8. 跨域命令识别 + 回滚：sidecar 4 条（用 `settings_manager`）、mcp 2 条（用 `ai_state`）保留 `AppState`
>
> **架构效果**：
> - **47 条 Tauri 命令**（26 sidecar + 7 pty + 5 sensitive-scan + 5 zap + 2 telemetry + 2 mcp + …）现在直接使用具体子状态，不再经过 monolithic `AppState`
> - `AppState` 仍保留为 "跨域编排容器"，承担真正需要多域访问的命令（AI init / sidecar apply-patch / MCP refresh 等）
> - 子状态与 `AppState` 共享底层 `Arc`，数据一致性零风险
> - `#[allow(dead_code)]` / `#[allow(unused_imports)]` 全部清零

**问题**：
`backend/crates/golish/src/state/mod.rs` 的 `AppState` 有 15 个字段。子状态（`DbState` / `PtyState` / `McpManaged` / `PentestToolState` / `SidecarManaged` / `TelemetryState`）已定义但全部 `#[allow(unused_imports)]`。

**检查命令**：
```bash
grep -c "allow(unused_imports)" backend/crates/golish/src/state/mod.rs
```
输出 > 0 = 未完成（每个 allow 都是一个未迁的子状态）

**执行步骤**（按子状态逐个做，每个 1 PR）：

1. **DbState 迁移**：
   - `grep -rn "State<'_, AppState>" backend/crates/golish/src/ | grep -i db`
   - 把 DB 相关命令的签名从 `State<'_, AppState>` 改为 `State<'_, DbState>`
   - 确认 DB 命令不再访问非-DB 字段
   - 从 `AppState` 删除 `db_pool` + `db_ready` 字段
   - 去掉 `DbState` 上的 `#[allow(unused_imports)]`

2. **PtyState 迁移**：同上（`pty_manager` / `pty_output_tap` / `active_terminal_session`）

3. **McpManaged 迁移**：同上（`mcp_manager`）

4. **PentestToolState 迁移**：同上（`pentest_config_manager` / `pentest_busy_sessions`）

5. **SidecarManaged 迁移**：同上（`sidecar_config` / `sidecar_state`）

6. **TelemetryState 迁移**：同上（`langfuse_active` / `telemetry_stats`）

**完成判据**：
- `grep -c "allow(unused_imports)" backend/crates/golish/src/state/mod.rs` = 0
- `AppState` 字段数 ≤ 3（仅留真正"跨域共享"的）
- `cargo check --workspace` 绿
- Tauri command 签名不再出现 `State<'_, AppState>`（除非确实需要全部字段）

**预计**：6-10h / 分 3-5 个 PR（每个子状态一个）

---

### Session B 流水线（前端 + 文档 + CI）

---

#### [ ] B1 · 拆 `frontend/mocks.ts` 4213 行

**问题**：
`frontend/mocks.ts` 4,213 行 / 136 KB，比整个 Zustand store 还大。

**决策**：用 `git mv` 按业务域拆 `frontend/__fixtures__/`。

**检查命令**：
```bash
wc -l frontend/mocks.ts 2>/dev/null | awk '{print $1}'
```
输出 > 800 = 未完成。

**执行步骤**：
1. 建 `frontend/__fixtures__/` 目录
2. 读 `frontend/mocks.ts` 整体结构，按注释块 / `export const` 分组
3. 按业务域拆成：
   - `frontend/__fixtures__/ai.ts`（agent messages / streaming / tool calls）
   - `frontend/__fixtures__/session.ts`（sessions / timelines）
   - `frontend/__fixtures__/pty.ts`（terminal outputs）
   - `frontend/__fixtures__/pentest.ts`（pentest mocks）
   - `frontend/__fixtures__/vuln-intel.ts`
   - `frontend/__fixtures__/git.ts`
   - `frontend/__fixtures__/common.ts`（辅助函数、通用类型）
4. 建 barrel：`frontend/__fixtures__/index.ts`
5. 原 `frontend/mocks.ts` 改为 `export * from './__fixtures__';`（保留作为 shim，2 周后删除）
6. 验证 build + test 绿

**完成判据**：
- `find frontend/__fixtures__ -name "*.ts" | xargs wc -l | sort -n | tail -1` 最大 ≤ 800 行
- `pnpm build` 绿
- `pnpm test:run` 绿
- `npx tsc --noEmit` 绿

**预计**：4-6h / 大 diff 但 git 能识别为 rename / 1 PR

---

#### [x] B2 · CI 加依赖图守门 — ✅ Done 2026-05-02

> ✅ **执行证据**：
> - `python3 scripts/check_dag.py` ✓ DAG clean across 43 crates
> - `bash scripts/check_file_sizes.sh` ✓ all files within size budget (with grandfather list)
> - `.github/workflows/arch-check.yml` 两个独立 job（DAG + file-sizes）
>
> **新增文件**：
> 1. `scripts/check_dag.py` (~150 行 Python)：
>    - 权威 LAYER_TABLE（与 `docs/architecture.md` 同源）
>    - 扫描 43 个 crate 的 `Cargo.toml`，过滤外部 crate
>    - 禁止**向上依赖**（dep_layer > crate_layer）
>    - 允许同层依赖（只要图无环）
>    - DFS 环检测（找出并格式化循环路径）
>    - 未知 crate 报错，强制作者把新 crate 放到 LAYER_TABLE
> 2. `scripts/check_file_sizes.sh` (~80 行 Bash)：
>    - Rust `.rs` 文件 ≤ 500 行（排除 tests/ / *_tests.rs / mocks.rs）
>    - TS/TSX 文件 ≤ 800 行（排除 __fixtures__/ / *.test.* / mocks.ts）
>    - **Grandfather 豁免函数** `ts_baseline()` 允许 5 个现有大文件按"当前行数"baseline，但**不允许继续增长**（CI 会红）
> 3. `.github/workflows/arch-check.yml`：pull_request + push main 触发
>
> **Grandfather 列表（未来优化目标）**：
> - `frontend/components/ToolManager/ToolManager.tsx` 1044 行
> - `frontend/components/VulnIntelPanel/WikiTab.tsx` 891 行
> - `frontend/components/SecurityView/ScanToolsPanel.tsx` 843 行
> - `frontend/components/ProjectOverview/ProjectOverview.tsx` 838 行
> - `frontend/components/VulnIntelPanel/VulnIntelPanel.tsx` 819 行
>
> **行为契约**：
> - 故意提交反向依赖的 PR → CI `dag` job 红
> - 故意加一个 501 行的 `.rs` → CI `file-sizes` job 红
> - 让 grandfather 里的文件继续增长 → CI `file-sizes` job 红
> - 新 crate 没加到 LAYER_TABLE → CI `dag` job 红（强制作者做分层决策）

**问题**：
无自动化手段阻止反向依赖合入，也无大文件体积守门。

**检查命令**：
```bash
ls .github/workflows/arch-check.yml 2>/dev/null && echo "EXISTS"
```
无 `EXISTS` 输出 = 未完成。

**执行步骤**：
1. 新建 `.github/workflows/arch-check.yml`
2. Job 1：**反向依赖守门**
   ```yaml
   - name: Install cargo-depgraph
     run: cargo install cargo-depgraph --locked
   - name: Generate dependency graph
     run: |
       cd backend
       cargo depgraph --workspace-only > /tmp/depgraph.dot
   - name: Check for illegal back-edges
     run: bash scripts/check-dag.sh /tmp/depgraph.dot
   ```
3. 新建 `scripts/check-dag.sh`：读 `depgraph.dot`，实现简单的层级白名单检查
4. Job 2：**大文件守门**
   ```yaml
   - name: No rust file > 500 lines
     run: |
       VIOLATORS=$(find backend/crates -name "*.rs" -not -path "*/tests/*" -not -name "*_tests.rs" | xargs wc -l | awk '$1>500 && $2!="total"')
       if [ -n "$VIOLATORS" ]; then echo "❌ files >500 lines:"; echo "$VIOLATORS"; exit 1; fi
   - name: No TS file > 800 lines
     run: |
       VIOLATORS=$(find frontend -name "*.ts" -o -name "*.tsx" | grep -v __fixtures__ | xargs wc -l | awk '$1>800 && $2!="total"')
       if [ -n "$VIOLATORS" ]; then echo "❌ files >800 lines:"; echo "$VIOLATORS"; exit 1; fi
   ```
5. 验证本地跑通：`bash scripts/check-dag.sh` 无错

**完成判据**：
- `.github/workflows/arch-check.yml` 存在且 PR 触发
- 故意提交一个反向依赖 PR → CI 能红
- 故意加一个 501 行的 .rs → CI 能红

**预计**：3-4h / ~150 行 diff / 1 PR

---

#### [x] B3 · 重写 `docs/architecture.md` — ✅ Done 2026-05-02

> ✅ **执行证据**：
> - `wc -l docs/architecture.md` = 248 行（从 23 行增长到 248 行）
> - 文档内所有提到的 crate 名全部与 workspace 实际一致（43 个活 crate + layer 分层）
> - 包含 ASCII 层级图 · 每层 crate 目录表 · 字段用途 · 演进原则 · 评估链接
>
> **涵盖内容**：
> 1. 技术栈速览表（11 项）
> 2. Repo layout（前端 + 后端 + e2e + docs 结构）
> 3. 6 层 DAG ASCII 层级图（L1 基础 → L6 Tauri app）
> 4. 每层 crate 目录表（含依赖 + 职责描述，共 43 crate 完整映射）
> 5. 前端 14 Zustand slice 说明
> 6. Tauri IPC 层 + A4 管理的 6 个子状态表
> 7. 演进原则（4 条 hard rule + CI 执行契约）
> 8. 相关文档链接（含本 refactor-execution 和 architecture-evaluation rule）

**前置**：必须在 Session A 的 A2 + A3 完成后做

**问题**：
`docs/architecture.md` 目前只说 3 层（`golish` / `golish-ai` / `golish-core`），实际 6 层 + 45 crate，对新贡献者误导。

**检查命令**：
```bash
wc -l docs/architecture.md
```
输出 < 50 = 未完成（目前是 23 行）

**执行步骤**：
1. 读当前 `docs/architecture.md`，保留"相关链接"部分
2. 画真实 6 层 DAG（用 mermaid `graph TD` 或 ASCII）：
   - L1 Foundation: `golish-core` / `-settings` / `-context` / `-models` / `-mcp`
   - L2 Domain: `golish-events` / `-session` / `-llm-providers` / `-tools`
   - L3 AI Primitives: `golish-prompts` / `-sub-agents` / `-indexer`
   - L4 Agent Runtime: `golish-agent-kit` → `golish-agent-runtime` → `golish-agent-bridge`
   - L5 Application: `golish` (Tauri entry)
3. 每个 crate 一行表：职责 · LOC · 稳定度（🟢 稳定 / 🟡 演进中 / 🔴 可能重构）· 主要 consumers
4. 章节「演进原则」：
   - 禁止反向依赖（CI 守门）
   - 单文件 ≤ 500 行（Rust）/ ≤ 800 行（TS）
   - 新 crate 必须 lib.rs 顶部写明分层与依赖
5. 章节「前端架构」：Zustand slice 化说明 + `__fixtures__/` 规范
6. 章节「IPC 层」：Tauri command 组织方式（命名约定 + 未来路由层计划）

**完成判据**：
- `docs/architecture.md` ≥ 200 行
- 文档内提到的所有 crate 与 workspace `Cargo.toml` 实际一致
- 包含 mermaid 或 ASCII 层级图

**预计**：3-4h / ~300 行新增 diff / 1 PR

---

#### [x] B4 · Tauri command namespace 设计 ADR（纸面）— ✅ Done 2026-05-02

> ✅ **执行证据**：
> - `docs/adr/0009-tauri-command-namespace.md` · ~160 行 · Status: Proposed
>
> **ADR 内容**：
> - Context：548 command 平铺 + `include!` hack + 无 namespace 的 5 个症状
> - 3 个候选方案的 tradeoff 分析：
>   - **A. 手工 domain bundles**（1 周 · 低风险 · 推荐先做）
>   - **B. `tauri::collect_commands!` + 前端 namespace 封装**（3-5 天 · 中风险）
>   - **C. build.rs 扫描 `#[tauri::command]` 生成 registry + TS 类型**（2-3 周 · 高风险）
> - Decision：**Defer 完整实施**，但立即采用 `<domain>_<verb>_<object>` 命名约定并写进 CONTRIBUTING.md
> - 推荐顺序：命名约定 → Option A → 视情况决定 B 或 C
> - 触发点：command 数达到 700 时重新评估

**问题**：
548 个 Tauri command 无 namespace，全部平铺。

**范围**：本任务**只出设计文档**，不动代码。

**检查命令**：
```bash
ls docs/adr/ | grep -i tauri-command-namespace
```

**执行步骤**：
1. 新建 `docs/adr/0XXX-tauri-command-namespace.md`
2. 内容包含：
   - Context：548 command 问题陈述
   - Decision：待定（本 ADR 给出 3 个方案对比）
   - 方案 A：按域拆成 `bundle`，`golish` 组合（手工）
   - 方案 B：使用 `tauri::collect_commands!` 宏自动收集（需研究 Tauri 2 是否支持）
   - 方案 C：生成代码（build.rs 扫描 `#[tauri::command]` 标注）
   - Trade-off 对比（维护成本 / 编译时间 / 前端体验 / 攻击面收敛）
   - Consequence：暂不实施，标为 Phase 3 候选
3. 提交 PR，邀 Review

**完成判据**：
- `docs/adr/0XXX-tauri-command-namespace.md` 存在且 ≥ 150 行

**预计**：2h / ~200 行 MD / 1 PR

---

## Phase 2: 核心重构（两 session 合并 · 3-5 天）

### [~] C1 · `run_agentic_loop_unified` → `TurnExecutor` 状态机（进行中：6/9 完成）— 2026-05-02

> 🟡 **进行中**：ADR + 骨架 + 第一个 phase（PreFlight）已落地
>
> ✅ **已完成**：
> 1. **C1-0 设计 ADR**：`docs/adr/0010-turn-executor-state-machine.md` · ~220 行
>    - 完整 Context / Decision / 10 phase 清单 / 8 步骤迁移计划
>    - 对比 3 个替代方案（as-is · actor · graph-flow DSL）
>    - 明确 trait：`TurnPhaseHandler` + `TurnInterceptor`
> 2. **C1-1 PoC · PreFlight phase**：
>    - 新建 `backend/crates/golish-agent-runtime/src/agentic_loop/turn/` 4 个文件：
>      - `mod.rs`（re-export）
>      - `state.rs`（`TurnState` struct，当前只有 `iteration` 字段）
>      - `phases/mod.rs`（`BreakReason` + `PhaseOutcome` enum）
>      - `phases/pre_flight.rs`（~70 行独立 phase 实现）
>    - 主 loop 的 3 段旧代码（`iteration += 1` / compaction reset / cancel 检查 / max_iter 检查）浓缩为一行 `pre_flight::run()` 调用
>    - **行为改进**：cancel/budget 检查现在先于 `pre_turn_compaction`，能更早响应用户中断
>
> **已通过判据**：
> - `cargo check --workspace --tests` 全绿 · 31s · 0 errors / 0 warnings
>
> **当前 mod.rs 行数变化**：495 → 483（-12 行；其余在 C1-2~C1-5 继续）
>
> ✅ **C1-2 Compaction phase 迁移**（2026-05-02）：
> - 新建 `turn/phases/compaction.rs`：根据 `iteration` 选 `pre_turn_compaction` / `inter_turn_compaction`，后者的 `TerminalErrorEmitted` 通过 `PhaseOutcome::Fail` 上抛
> - 扩展 `PhaseOutcome` 枚举，新增 `Fail(anyhow::Error)` 变体（之前只有 Continue/Break）
> - 主 loop 的 compaction 分支从内联 if-else 变为单个 `match compaction_phase::run(...)` 语句（~8 行 → 10 行，语义清晰度增）
> - `use turn::{compaction as compaction_phase, ...}` 用别名避开与 sibling `agentic_loop::compaction` mod 的名字冲突
> - `cargo check --workspace --tests` 全绿 · 30s · 0 errors / 0 warnings
>
> ✅ **C1-3 FirstIterHooks + TokenEstimate phase 迁移**（2026-05-02）：
> - 新建 `turn/phases/first_iter_hooks.rs`：条件分支 `iteration == 1 && !is_sub_agent` 封装进 phase；调用 `run_first_iteration_hooks`，把 `reflector_active` 输出写回 `TurnState`
> - 新建 `turn/phases/token_estimate.rs`：主动估算 input tokens + 写 `compaction_state`（~20 行）
> - `TurnState` 新增 `reflector_active: bool` 字段（`Default = true`）
> - 主 loop 对应 15+15 行内联代码压缩为两个 `match phase::run(...)` 调用
> - `use run_first_iteration_hooks` / `use estimate_message_tokens` 从 mod.rs 移除；后者保留 `#[cfg(test)]` 给 tests 模块继承
> - `cargo check --workspace --tests` 全绿 · 30s · 0 errors / 0 warnings
>
> ✅ **C1-4 Completion phase 迁移**（2026-05-02）：
> - 新建 `turn/phases/completion.rs`（~130 行）：把原 mod.rs 主 loop 三段（Langfuse `llm_completion` span 创建 + `start_completion_stream` + `process_stream`）合并为一个 phase；返回 `CompletionOutcome::Continue { outcome, llm_span }` 携 span 给下游 phase 用，或 `CompletionOutcome::BreakAgentLoop` 让 scheduler 终止
> - `start_completion_stream` / `StreamProcessOutcome` / `StreamOutcome` / `process_stream` 的可见性从 `pub(super)` 提升到 `pub(crate)`（让 `turn::phases::completion` 能调用 sibling 模块）
> - 主 loop 用 `match completion_phase::run(...).await? { Continue { outcome, llm_span } => (outcome, llm_span), BreakAgentLoop => break }` 一次性吃掉旧的 ~50 行 span+stream 代码
> - mod.rs 同步清理：删 `llm_stream_start::start_completion_stream` / `stream_processor::{process_stream, StreamOutcome}` 旧 import；turn 块用 `completion::{self as completion_phase, CompletionOutcome}` 嵌套式 use；删 `BreakReason` re-export（C1-5+ 需要再加）；解构改 `let StreamProcessOutcome { ... }`
> - mod.rs 行数：460 → **457 行**（中间 -50 行旧逻辑 +47 行 phase 调用 + 注释；净 -3 行；剩余压缩在 C1-6）
> - `cargo check --workspace --tests` 全绿 · 17s · 0 errors / 0 warnings
> - `python3 scripts/check_dag.py` ✓ DAG clean across 43 crates
> - `bash scripts/check_file_sizes.sh` ✓ all files within size budget
>
> ✅ **C1-5 AssistantPush + ReflectorOrBreak + ToolDispatch phase 迁移**（2026-05-02）：
> - 新建 3 个 phase 文件：
>   - `turn/phases/assistant_push.rs`（~43 行）：透传到 `assistant_message::push_assistant_message`，作为后续 hook 插点
>   - `turn/phases/reflector_or_break.rs`（~75 行）：处理 no-tool-call 分支；新引入本地 `ReflectorPhaseOutcome` 枚举（`Continue` / `Repeat` / `Break`），不污染通用 `PhaseOutcome`
>   - `turn/phases/tool_dispatch.rs`（~68 行）：allow-list 过滤 + `push_unavailable_tool_results` + `dispatch_tool_calls`
> - `TurnState` 新增两个字段：`consecutive_no_tool_turns: u32`、`total_reflector_nudges: u32`，更新 doc 注释明确"loop-wide"语义
> - widen 5 个 helper 可见性：`push_assistant_message` / `maybe_run_reflector` / `ReflectorOutcome` / `dispatch_tool_calls` / `push_unavailable_tool_results` 从 `pub(super)` 升 `pub(crate)`
> - 主 loop 三块（push_assistant_message + reflector match + allow-list partition + dispatch）压缩为三个 `phase::run(...)` 调用（match `ReflectorPhaseOutcome::Repeat => continue, Break => break`）
> - mod.rs 同步清理：删 `assistant_message::push_assistant_message` / `reflector::{maybe_run_reflector, ReflectorOutcome}` / `tool_dispatch::dispatch_tool_calls` / `unified_helpers::push_unavailable_tool_results` 旧 import；删 `consecutive_no_tool_turns` / `total_reflector_nudges` local var；turn re-export 加 `assistant_push_phase`、`reflector_or_break_phase`、`tool_dispatch_phase`、`ReflectorPhaseOutcome`
> - mod.rs 行数：457 → **435 行**（-22 行；剩余压缩到 ≤150 在 C1-6 调度器化时一次性完成）
> - `cargo check --workspace --tests` 全绿 · 38s · 0 errors / 0 warnings
> - `python3 scripts/check_dag.py` ✓ DAG clean across 43 crates
> - `bash scripts/check_file_sizes.sh` ✓ all files within size budget
>
> ⏳ **剩余 3 个里程碑**（每个独立 PR · ≤500 行 diff）：
>
> | PR | 工作 |
> |---|---|
> | C1-6 | 主 `loop` 替换为 phase 调度器 · mod.rs 目标 ≤150 行 |
> | C1-7 | 引入 `TurnInterceptor` trait，Langfuse/HITL 搬出 phase body |
> | C1-8 | 每 phase 补 1+ 单元测试（mock `TurnState` + 协作者） |
>
> **预计剩余耗时**：1 天 · 3 个 PR

**前置**：Phase 1 全部完成

**问题**：
`backend/crates/golish-agent-runtime/src/agentic_loop/mod.rs`（改名后路径）里 `run_agentic_loop_unified` 是 300 行上帝函数，12 个职责编排固化。

**设计**：
1. 定义 `TurnPhase` 枚举：
   ```rust
   enum TurnPhase {
       Pre,           // 取消检查 + compaction + first-iter hooks
       StreamStart,   // token estimate + LLM stream 启动
       StreamProcess, // 接收 delta + 累积响应
       PostMessage,   // push assistant message
       ToolDispatch,  // allow-list + dispatch
       PostTurn,      // record completion
   }
   ```
2. 定义 `TurnExecutor` trait：
   ```rust
   #[async_trait]
   trait TurnExecutor {
       async fn execute_phase(&mut self, phase: TurnPhase, ctx: &mut TurnContext<'_>) -> TurnOutcome;
   }
   ```
3. `TurnInterceptor` trait for Reflector / Compaction / HITL：
   ```rust
   #[async_trait]
   trait TurnInterceptor {
       async fn before(&self, phase: TurnPhase, ctx: &TurnContext<'_>);
       async fn after(&self, phase: TurnPhase, ctx: &TurnContext<'_>, outcome: &TurnOutcome);
   }
   ```
4. mod.rs 只做 phase 编排与 interceptor 调用；每个 phase 在独立文件实现；每个 phase 可独立单测（给 mock context）。

**完成判据**：
- `backend/crates/golish-agent-runtime/src/agentic_loop/mod.rs` ≤ 150 行
- 每个 phase 有独立的 `phase_{name}_tests.rs`
- `cargo test -p golish-agent-runtime` 绿
- 现有 e2e 测试（Playwright）全绿

**预计**：3-5 天 / 拆成 5-6 个 PR

---

## 时间线 + 协同节点

```
    Day 1-2  │ A: A1 (反向依赖)                │ B: B1 (拆 mocks.ts)
    Day 3-4  │ A: A2 (改名)         ─┐         │ B: B2 (CI 守门)
    Day 5    │ A: A3 (杀 umbrella)  │notify→   │ B: B3 (写架构文档)
    Day 6-8  │ A: A4 (AppState 迁移) │         │ B: B4 (ADR 纸面)
    ──────────────────────────────────────────────────────
    Day 9-14 │                    合并做 C1 (TurnExecutor 状态机)
```

**协同信号**：
- A2 完成后，Session A 在本文件对应 `[x]` 后追加 `⚡ A2-DONE 通知 Session B`
- Session B 开始 B3 前，检查本文件是否有 `⚡ A2-DONE`

---

## 完成度仪表盘（每周一更新一次）

| 任务 | 负责 Session | 状态 | PR | 完成日期 |
|---|---|---|---|---|
| A1 反向依赖 | A | ✅ [x] | 待提交 | 2026-05-02 |
| A2 agent 层改名 | A | ✅ [x] | 待提交 | 2026-05-02 |
| A3 杀 umbrella | A | ✅ [x] | 待提交 | 2026-05-02 |
| A4 AppState 迁移 | A | ✅ [x] | 待提交 | 2026-05-02 |
| B1 拆 mocks.ts | B | [ ] | - | - |
| B2 CI 守门 | B | ✅ [x] | 待提交 | 2026-05-02 |
| B3 重写架构文档 | B | ✅ [x] | 待提交 | 2026-05-02 |
| B4 Tauri ADR | B | ✅ [x] | 待提交 | 2026-05-02 |
| C1 TurnExecutor | A+B | 🟡 [~] 6/9 | 待提交 | 2026-05-02 |

**全部完成 = Golish B+ → S+ 迁移完成**
