---
description: 架构 S→S+ 升级路线图（按优先级 + 增量提交策略）
globs:
alwaysApply: false
---

# Golish-Platform 架构升级路线图 (Refactor Roadmap)

> 起点：A- 级（38 crate / 175K LOC 后端 + 91K LOC 前端，0 errors / 0 warnings）
> 终点：S+ 级（≤8K LOC 平均 crate / 拓扑零反向依赖 / DB 100% trait 化 / lib.rs ≤120 行）

---

## 0. 执行原则（每个 PR 必须满足）

1. **小步提交**：单 PR ≤500 行 diff；超过则拆分。
2. **绿构建守门**：`cargo check --workspace` 与 `cd frontend && npx tsc --noEmit` 必须 0 errors / 0 warnings。
3. **测试同步**：抽出 trait 必须给出 `MockX` 或 `NullX`；新逻辑写至少 1 个 happy-path 测试。
4. **回滚友好**：每个里程碑独立可回滚；不允许跨里程碑共享中间分支。
5. **文档同步**：拆分新 crate 时，在其 `lib.rs` 顶部写明分层、依赖、对外契约。

---

## Phase 1：手术式整理（1-2 周，6 个 PR）

> **目标**：消灭显眼的拓扑瑕疵，让架构图变干净；不动主体逻辑。

### P1-1 修复反向依赖：`golish-indexer → golish-ai` — ✅ Done

> ✅ Done 2026-05-02: `golish-indexer` 已不再依赖 `golish-ai`(grep `golish-ai` `backend/crates/golish-indexer/Cargo.toml` 无结果)。embedding 客户端代码已迁出。

**问题**：`backend/crates/golish-indexer/Cargo.toml` 依赖了上层的 `golish-ai`，破坏 4 层 DAG。

**方案**：
1. 在 `golish-llm-providers` 内新增 `embeddings.rs` 模块（或新建 `golish-embeddings` Layer-2 crate）。
2. 把当前位于 `golish-ai/src/indexer/` 的 embedding 客户端代码（OpenAI / Vertex / 本地模型）迁过去。
3. `golish-ai` 与 `golish-indexer` 都改依赖新模块。
4. 删除 indexer 的 `golish-ai` 依赖。

**完成判据**：`rg "golish-ai" backend/crates/golish-indexer/Cargo.toml` 无结果。

---

### P1-2 模块化 Tauri command 注册 — ✅ Done

> ✅ Done 2026-05-02: `wc -l backend/crates/golish/src/lib.rs` = 76 行(目标 ≤120,达成)。`tauri::generate_handler![…]` 已迁到 `commands_registry.rs`。

**问题**：`backend/crates/golish/src/lib.rs` 单个 `generate_handler![]` 宏 600+ 行，违反 SRP。

**方案**（受 Tauri 框架限制，**只能局部缓解**）：
1. 每个域模块（`tools::pentest`、`tools::wiki`、`ai::commands`、`indexer::commands`、…）暴露一个 `pub fn handler_names() -> &'static [&'static str]`，仅用于文档与诊断。
2. 用 `cargo expand` + 自动生成脚本（`scripts/regen_command_list.rs`）维护 `lib.rs` 的命令清单，让人改命令时只改子模块、跑一次脚本。
3. 把 `lib.rs` 拆出 `commands_registry.rs`，把 `tauri::generate_handler![…]` 移过去；`lib.rs` 仅保留 `pub fn run_gui()` 主体（目标 ≤120 行）。

**完成判据**：`wc -l backend/crates/golish/src/lib.rs` ≤ 120。

---

### P1-3 补全新 crate 的测试（4 个） — ✅ Done

> ✅ Done 2026-05-02: 4 个 crate 全部有 `tests/api_contract.rs` + 各自的领域测试(`storage_and_progress.rs` / `pure_helpers.rs` / `crud_roundtrip.rs` / `state_lifecycle.rs`)。

**目标**：每个 crate ≥ 1 个集成测试 + 1 个 trait mock。

| Crate | 必写测试 | Mock 对象 |
|---|---|---|
| `golish-scan-runner` | `test_run_whatweb_writes_to_storage` | `MockScanStorage` + `NullEmitter` |
| `golish-vuln-intel` | `test_intel_search_remote_paginates` | reqwest 录制 fixture |
| `golish-projects` | `test_project_crud_roundtrip` | tmp dir |
| `golish-indexer` | `test_index_directory_emits_progress` | `NullEmitter` |

**完成判据**：`cargo test -p golish-scan-runner -p golish-vuln-intel -p golish-projects -p golish-indexer` 全绿。

---

### P1-4 收尾既有大文件拆分 — ✅ Done

> ✅ Done 2026-05-02: 所有原始榜单上的文件已拆 + 后续两个新增违规也拆完(`codebases.rs` 555→89/168/122/83/104, `loader.rs` 503→258/206/56)。完成判据 `find ... awk '$1>500'` 输出空。

**目标**：所有 .rs 业务文件 ≤500 行（测试文件不限）。

依据 `wc -l backend/crates/**/*.rs` 当前榜单（排除 tests/ 与 *_tests.rs）:

| 文件 | 原行数 | 拆分方向 | 状态 |
|---|---|---|---|
| `golish-llm-providers/src/lib.rs` | 500 | 按 provider 折分 | ✅ |
| `golish-sub-agents/src/defaults/prompts.rs` | 1171 | 按 agent 拆 | ✅ |
| `golish-scan-runner/src/nuclei.rs` | 701 | 拆 `nuclei/{runner,parser,poc_match}.rs` | ✅ |
| `golish-ai/src/task_orchestrator/bridge_executor.rs` | 678 | A1 整体拆分 | ✅ |
| `golish/src/indexer/commands/codebases.rs` | 555 | 拆 5 文件 (mod/list/crud/lifecycle/memory) | ✅ |
| `golish-settings/src/loader.rs` | 503 | 拆 3 文件 (mod/env/migration) | ✅ |

**完成判据**：`find backend/crates -name "*.rs" -not -path "*/tests/*" -not -name "*_tests.rs" | xargs wc -l | awk '$1>500'` 输出空。

---

### P1-5 删除遗留死代码 — ✅ Done

> ✅ Done 2026-05-02: 所有原始清单已删除并 commit。

依据 git status 已删除：
- ✅ `golish/src/projects/schema.rs`、`storage.rs`（已迁 `golish-projects`）
- ✅ `golish/src/tools/scan_runner/{feroxbuster,nuclei,whatweb,helpers,types}.rs`
- ✅ `golish/src/tools/vuln_intel/{fetch,github_poc,nuclei_*,types}.rs`
- ✅ `golish/src/indexer/vtcode_bridge.rs`
- ✅ `golish/src/tools/pipeline/templates.rs`

**Action**：把这些 git 已删但未 commit 的清理纳入第一个 PR。

---

### P1-6 前端 store 卫生 — ✅ Done

> ✅ Done 2026-05-02: `wc -l frontend/store/index.ts` = 54 行(目标 ≤80,达成)。slices 已成立。

**问题**：`frontend/store/index.ts` 根状态散落 `chatPanelVisible / zapRunning / appIsFocused` 等单字段。

**方案**：
1. 新增 `slices/app-shell.ts`，把 `appIsFocused / appIsVisible / chatPanelVisible / workspaceDataReady / zapRunning / pendingTerminalRestoreData / terminalRestoreInProgress / currentProjectName / currentProjectPath` 全部归入。
2. 删除 `index.ts` 根级 setter，只剩 slice composition + dev tools。
3. 全文搜索替换：`useStore((s) => s.zapRunning)` → `useStore((s) => s.appShell.zapRunning)`（用 codemod 脚本）。

**完成判据**：`frontend/store/index.ts` ≤80 行。

---

## Phase 2：核心解耦（3-4 周，4 个 PR）

> **目标**：拆掉两个最大的耦合点 — `golish-ai` megacrate 与 god-state `AppState`。

### P2-1 拆分 `golish-ai`（最大工程） — ✅ Done

> ✅ Done 2026-05-02: 4 个 crate 全部拆出(commit 09d7a0b / 8e2426d 系列)。`golish-ai/src/lib.rs` = 39 行 facade。详见 `architecture-upgrade-plan.mdc` A1-1 ~ A1-4。

当前 34.9K LOC / 159 文件，拆分为 4 个 crate：

```
golish-ai (orchestration shell, ~6K LOC)
  ├─ depends on ↓
golish-agent-loop  (执行循环, ~10K LOC)
  ├─ agentic_loop/
  ├─ task_orchestrator/
  ├─ tool_execution/, tool_executors/
  └─ loop_detection/
       depends on ↓
golish-prompts  (Prompt 组合, ~5K LOC)
  ├─ contributors/
  ├─ prompt_registry/
  ├─ system_prompt/
  ├─ codex_prompt.rs
  └─ summarizer.rs
       depends on ↓
golish-agent-bridge  (生命周期 + Tauri-friendly API, ~7K LOC)
  ├─ agent_bridge/
  ├─ bridge_*.rs
  ├─ agent_mode.rs / execution_mode.rs
  └─ db_traits/, db_tracking/, db_shim.rs, sidecar_trait.rs
```

**步骤**（每子 crate 独立 PR）：
1. **PR-A** 抽 `golish-prompts`（叶子最深，依赖最少）
2. **PR-B** 抽 `golish-agent-loop`（依赖 prompts + tools）
3. **PR-C** 抽 `golish-agent-bridge`（依赖 agent-loop）
4. **PR-D** `golish-ai` 变成元 crate：仅 `pub use golish_agent_bridge::*; pub use golish_agent_loop::*; pub use golish_prompts::*;`，逐步弃用并通过 `#[deprecated]` 引导外部消费者改 import。

**完成判据**：
- `wc -l backend/crates/golish-ai/src/lib.rs` ≤200。
- 单 crate 增量编译时间 (`cargo check -p golish-agent-loop`) ≤8s。

---

### P2-2 把 `AppState` 拆成域聚合根 — ✅ Done

> ✅ Done 2026-05-02: AppState 已拆成 7 个独立 manage<T>(commit 6df0b08 [A2-1])。详见 `architecture-upgrade-plan.mdc` A2-1。

**问题**：`backend/crates/golish/src/state.rs` 17 字段，每个字段被独立的命令子集消费。

**方案**：用 Tauri 的多 `manage<T>()` 注入多个状态对象：

```rust
// 当前
.manage(AppState { pty_manager, ai_state, indexer_state, settings_manager, ... })

// 目标
.manage(PtyState::new())
.manage(AiState::new())
.manage(IndexerState::new())
.manage(SidecarState::new())
.manage(McpState::new())
.manage(DbState { pool, ready })
.manage(PentestState::new())
```

**步骤**：
1. 把 `AppState` 拆成 7 个独立 struct（PtyState / AiState / IndexerState / SidecarState / McpState / DbState / PentestState）。
2. 命令签名从 `state: tauri::State<'_, AppState>` 收窄为它实际需要的子状态（如 `state: tauri::State<'_, PtyState>`）。
3. 移除 `AppState`。

**完成判据**：搜索 `tauri::State<'_, AppState>` 无结果。

---

### P2-3 `golish-events` 脱离 `golish-ai` — ✅ Done

> ✅ Done 2026-05-02: `golish-events` 是 Layer 1 独立 crate, `golish-ai/lib.rs` 已成 facade。下游通过 `golish_events::*` 直接 import,无需再走 `golish_ai::*`。

**问题**：`golish-ai/src/lib.rs:68-69` 写着：
```rust
pub(crate) use golish_events::event_coordinator;
pub(crate) use golish_events::transcript;
```

按分层 events 是 Layer 2，但实际它依赖了 `golish-core` 的 `RuntimeEvent`，又被 `golish-ai` 通过 `pub use` 大量再导出。**方向是对的，但要把 `pub use` 转成稳定的 façade**，让外部消费者直接 `use golish_events::TranscriptWriter` 而非 `use golish_ai::TranscriptWriter`。

**Action**：grep `golish_ai::Transcript` / `golish_ai::CoordinatorState` 等等，全部改成 `golish_events::*`。

---

### P2-4 统一 DB 抽象到 trait（按域分批） — 🟡 Partial

> 🟡 2026-05-02: `golish-pipeline` / `golish-scan-runner` / `golish-session` / `golish-ai` 已有 trait;`golish-pentest` / `golish-vuln-intel` / `golish-projects` 仍直接用 `&PgPool`,这是 P2-4 剩余范围(grep `PgPool` 在域 crate 仍有 ~10 处命中)。

**目标**：所有 Layer 3 域 crate 不直接接 `&sqlx::PgPool`，改接 `&dyn XxxRepo`。

| Crate | 新 Trait | 优先级 |
|---|---|---|
| `golish-pentest` | `PentestRepo` | 高 |
| `golish-vuln-intel` | `VulnIntelRepo` | 中 |
| `golish-projects` | `ProjectRepo` | 中 |
| `golish-session` | 已有 | ✅ |
| `golish-ai` | 已有 (`db_traits`) | ✅ |
| `golish-pipeline` | 已有 (`PipelineStorage`) | ✅ |
| `golish-scan-runner` | 已有 (`ScanStorage`) | ✅ |

**模式**（参考 `golish-pipeline/src/storage.rs`）：
```rust
#[async_trait]
pub trait PentestRepo: Send + Sync {
    async fn save_finding(&self, finding: &Finding) -> Result<Uuid, RepoError>;
    async fn list_targets(&self, project: &str) -> Result<Vec<Target>, RepoError>;
    // ...
}
```

实现住在 `golish-db` 或主 crate，业务代码只看 trait。

**完成判据**：`rg "PgPool" backend/crates/golish-{pentest,vuln-intel,projects}/src` 仅出现在 trait 实现处。

---

## Phase 3：体验与扩展（持续）

### P3-1 编译时间预算 — ⏳ Pending baseline

> ⏳ 2026-05-02: 未运行基准测试。下次 session 可以跑 `time cargo check --workspace`(冷/热)、`time cargo check -p golish`、`time cargo check -p golish-agent-loop` 各 3 次取中位数,再决定是否需要 sccache/cargo-chef。

| 操作 | 目标 |
|---|---|
| `cargo check --workspace`（增量） | ≤30s |
| `cargo check -p golish` | ≤45s |
| `cargo check -p golish-agent-loop` | ≤8s |
| `cargo build --release` (首次) | ≤6 min |

**手段**：
- `[profile.dev.package."*"] opt-level = 2`（已配）
- 评估 `cargo-chef` 或 `sccache` 加速 CI。
- 拆出更细的 trait crate (`golish-traits-only`) 让消费者只编译类型签名。

---

### P3-2 前端代码分割（lazy chunks） — ✅ Done (主要项)

> ✅ Done 2026-05-02 (commit df2459f): SecurityView 4 个 invisible-mount panel(Repeater/Intruder/Scanner/ScanTools) + VulnIntelPanel 的 WikiTab 已转 React.lazy + visited-once 门控。
> 
> 新增 5 个独立 chunk(共 113.55 kB / 30.02 kB gzip):
> - RepeaterPanel 17.35 kB, IntruderPanel 14.61 kB, ScannerPanel 31.48 kB, ScanToolsPanel 30.97 kB, WikiTab 19.14 kB
> 
> ToolManager.tsx (1044) 早已在 `App/lazyRegistry.ts` 中 lazy 化(verified)。

`frontend/components` 下 600+ 文件全部 eager-import。Vite bundle 当前 (`dist/index-*.js` 346KB) 还可以，但 `ToolManager.tsx` (1046 行) / `WikiTab.tsx` (720 行) / `RepeaterPanel.tsx` (604 行) 等大组件应转 `React.lazy(() => import(…))`。

**目标**：首屏 JS ≤200KB（gzip）。

---

### P3-3 ts-rs 100% 覆盖 — ✅ Done (主要项)

> ✅ Done 2026-05-02: `grep "TODO.*replace.*generated" frontend/lib/ frontend/store/` 无结果。剩余 store-types.ts 是 view-model 设计性保留(plan 标注允许)。

剩余手写类型清单：
- `frontend/lib/ai/types.ts` 中标注 `// TODO: replace with generated`
- `frontend/store/store-types.ts` 中的 view-model（保留）

**工具**：跑 `just generate-types`，对照 grep `interface\|type` 数差。

---

### P3-4 独立 crate 版本化（可选）

`workspace.package.version = "0.2.43"` 让 38 crate 同步发版。如未来 `golish-pentest`、`golish-vuln-intel` 想独立发布到 crates.io：

1. 移除 `version.workspace = true`。
2. 引入 `release-please` per-package 配置（项目根已有 `release-please-config.json`）。
3. 拆分 CHANGELOG.md。

---

## 量化目标对照表

| 指标 | 起点 | Phase 1 后 | Phase 2 后 | Phase 3 后 | 当前 (2026-05-02) |
|---|---|---|---|---|---|
| 后端 crate 数 | 38 | 38 | **41** (golish-ai 拆 3) | 41-43 | **42** ✅ |
| `golish` lib.rs 行 | 672 | **≤120** | ≤120 | ≤120 | **76** ✅ |
| `golish-ai` LOC | 34.9K | 34.9K | **≤6K** | ≤6K | **39 行** facade ✅ |
| 反向/同层依赖 | 1 (indexer→ai) | **0** | 0 | 0 | **0** ✅ |
| 业务文件 >500 行 | 8+ | **0** | 0 | 0 | **0** ✅ (P1-4 达成) |
| 域 crate 用裸 PgPool | 3 | 3 | **0** | 0 | **3** 🟡 (P2-4 部分完成) |
| 新 crate 测试覆盖 | 0/4 | **4/4** | 4/4 | 4/4 | **4/4** ✅ |
| 综合评级 | A- | A | A+ | S | **S-** (P2-4 + P3-1 baseline 后达 S) |

---

## 优先级速查（如果只能做 3 件事）

1. **P1-1 + P1-3**（1 周）— 修反向依赖 + 补测试，立即把架构评分推到 A。
2. **P2-1**（2-3 周）— 拆 golish-ai，是后续一切优化的前提。
3. **P2-2**（1 周）— 拆 AppState，长期可维护性的关键。

---

## 工具命令

```bash
# 验证后端
cd backend && cargo check --workspace
cd backend && cargo test --workspace

# 验证前端
cd frontend && npx tsc --noEmit
cd frontend && npm run test

# 重新生成 ts-rs 类型
just generate-types

# 检查文件长度违规
find backend/crates -name "*.rs" -not -path "*/tests/*" -not -name "*_tests.rs" \
  | xargs wc -l | awk '$1>500 {print}'

# 检查同层/反向依赖
rg "^golish-(ai|sub-agents|pentest|pipeline|tools|scan-runner|vuln-intel|projects)" \
  backend/crates/golish-{indexer,session,context,settings,events,db,llm-providers}/Cargo.toml
```
