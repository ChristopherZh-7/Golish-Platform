---
description: Frontend + Backend A+ upgrade plan - remaining tasks
globs:
alwaysApply: false
---

# A+ 升级计划

## 已完成 (A 级)
- ✅ 后端 3 个 crate (golish-ai, golish-sub-agents, golish-session) 完全移除 sqlx
- ✅ 7 个 trait 抽象 + 8 个应用层实现
- ✅ 前端 16 个 API 模块，组件零 invoke（除 DetachedView 窗口管理）
- ✅ 0 errors, 0 warnings
- ✅ golish-db 重复 ready_gate.rs 已删除
- ✅ ts-rs 类型生成基础设施已存在 (`frontend/lib/generated/`)
- ✅ 6 个类型已迁移到 ts-rs 生成：StepStatus, PlanStep, PlanSummary, ApprovalPattern, ToolSource, RiskLevel

## 剩余 A+ 任务

### 任务 1: 全面集成 ts-rs 生成的类型 (部分完成)

**问题**: `frontend/lib/generated/` 已有 11 个生成的类型文件，但前端大部分仍用手动定义的类型。

**主要障碍**: 命名约定不匹配 — 后端 snake_case vs 前端 camelCase。
- 生成的 `ToolSource` 用 `agent_id`，手动的 `ToolCallSource` 用 `agentId`
- 生成的 `AiEvent` 字段是 snake_case，但事件处理器 (`services/ai-events/`) 用 camelCase

**已完成 (7 个类型)**:
- ✅ AiEvent — 280 行手动类型替换为生成类型 + 信封交叉类型
- ✅ ToolSource, ApprovalPattern, RiskLevel, StepStatus, PlanStep, PlanSummary

**S 级扩展 — 剩余需生成的类型（按 Rust crate 分组）**:

**golish-core 需要加 `#[derive(TS)]` 的类型**:
- `ApiRequestStats` / `ApiRequestStatsSnapshot` → 前端 `lib/ai/types.ts`
- Tauri command 返回类型（在 golish crate 的 commands/ 中定义）

**golish-settings 需要加的类型**:
- `GolishSettings`, `AiSettings` 的子类型 → 前端 `lib/ai/types.ts` 的 config 类型

**golish-session 需要加的类型**:
- `GolishSessionSnapshot`, `SessionListingInfo`, `GolishSessionMessage` → 前端的 session 类型

**注意**: 前端 `lib/ai/types.ts` 中很多类型（如 `ProjectSettings`, `AiConfig`, 
`ProviderConfig` 等）是纯前端视图模型，没有 Rust 对应类型——这些不需要生成。

**步骤（每组独立可执行）**:
1. 在 Rust 类型上加 `#[derive(TS)]` + `#[ts(export, export_to = "generated/")]`
2. 运行 `just generate-types` 
3. 在前端 `import type { X } from "@/lib/generated"` 替换手动定义
4. 验证 `npx tsc --noEmit`

**关键文件**:
- `backend/crates/golish-core/src/events/event.rs` — AiEvent 定义，添加 ts() 属性
- `backend/crates/golish-core/src/events/tool_source.rs` — ToolSource 定义
- `backend/crates/golish-core/src/hitl.rs` — ApprovalPattern 定义
- `backend/crates/golish-core/src/plan.rs` — PlanStep, PlanSummary, StepStatus 定义
- `frontend/hooks/tauri-event-types.ts` — 手动的 TauriAiEvent 类型
- `frontend/services/ai-events/types.ts` — 事件处理器类型
- `frontend/store/types/tool-call.ts` — ToolCallSource 手动类型

### 任务 2: 大组件评估 (已评估 — 当前架构合理)

**需拆分的组件**:

1. **ToolManager.tsx (1046 行) — 已评估，不拆分**
   - 已有良好的 hook 分离：useToolData, useToolEditor, useToolInstall, useSkillEditor, useGithubImport
   - 对话框已提取到 Dialogs.tsx
   - 编辑器字段已提取到 OutputParserEditor.tsx
   - 主文件是纯 JSX 布局/组合层，拆分会导致过度 prop drilling

2. **PentestEnvSettings.tsx (925 行)**
   - 拆分为：
     - `PentestEnvSettings/index.tsx` — 主容器
     - `PentestEnvSettings/RuntimeSection.tsx` — 运行时管理
     - `PentestEnvSettings/PythonEnvSection.tsx` — Python 环境

3. **DashboardPanel.tsx (849 行)**
   - 拆分为：
     - `DashboardPanel/index.tsx` — 主容器 + 布局
     - `DashboardPanel/StatCards.tsx` — 统计卡片
     - `DashboardPanel/ActivityFeed.tsx` — 活动信息流

### 任务 3: 前端错误处理 (已评估 — 当前模式合理)

**评估结果**: 
- `.catch(console.error)` 用于火-and-forget 乐观更新（26处），这是合理的模式
- `try/catch` + 用户通知用于关键操作（安装、删除等），已正确处理
- 当前不需要统一 — 不同场景用不同策略是正确的

### 运行命令
```bash
# 重新生成 ts-rs 类型
cd backend && just generate-types

# 验证前端编译
cd .. && npx tsc --noEmit

# 验证后端编译  
cd backend && cargo check
```
