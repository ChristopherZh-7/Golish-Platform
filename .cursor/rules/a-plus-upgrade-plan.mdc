---
description: Frontend + Backend A+ upgrade plan - remaining tasks
globs:
alwaysApply: false
---

# A+ 升级计划

## 已完成 (A 级)
- ✅ 后端 3 个 crate (golish-ai, golish-sub-agents, golish-session) 完全移除 sqlx
- ✅ 7 个 trait 抽象 + 8 个应用层实现
- ✅ 前端 16 个 API 模块，组件零 invoke（除 DetachedView 窗口管理）
- ✅ 0 errors, 0 warnings
- ✅ golish-db 重复 ready_gate.rs 已删除
- ✅ ts-rs 类型生成基础设施已存在 (`frontend/lib/generated/`)
- ✅ 65 个类型已迁移到 ts-rs 生成（全面覆盖 4 个 crate）
- ✅ 大组件拆分：PentestEnvSettings (4 文件)、DashboardPanel (3 文件)

## 剩余 A+ 任务

### 任务 1: 全面集成 ts-rs 生成的类型 (✅ 已完成)

**已完成**: 65 个类型文件已自动生成，覆盖 4 个 Rust crate：

**golish-core (14 类型)**:
- ✅ AiEvent, ToolSource, ApprovalPattern, RiskLevel, StepStatus, PlanStep, PlanSummary
- ✅ TaskPlan, ApprovalDecision, ToolApprovalConfig
- ✅ PromptPart, PromptPayload
- ✅ ApiRequestStatsSnapshot, ProviderRequestStatsSnapshot

**golish-session (4 类型)**:
- ✅ GolishMessageRole, GolishSessionMessage, GolishSessionSnapshot, SessionListingInfo

**golish-settings (37 类型)**:
- ✅ GolishSettings + 所有子类型 (AiSettings, NetworkSettings, etc.)
- ✅ 所有 LLM provider settings (VertexAi, OpenRouter, Anthropic, OpenAI, etc.)
- ✅ 所有 enum (Theme, LogLevel, IndexLocation, ReasoningEffort)
- ✅ ProjectSettings, ProjectAiSettings

**golish-models (3 类型)**:
- ✅ OwnedModelDefinition, DynamicModelDefinition, ModelCapabilities

**剩余可选优化**:
- 前端 `lib/ai/types.ts` 中的手动类型可逐步替换为 `import type { X } from "@/lib/generated"`
- 前端纯视图模型 (AiConfig, ProviderConfig 等) 不需要替换

### 任务 2: 大组件拆分 (✅ 已完成)

1. **ToolManager.tsx (1046 行) — 不拆分** (已有良好的 hook 分离)

2. **PentestEnvSettings.tsx (925 行) → 4 文件** ✅
   - `PentestEnvSettings/index.tsx` — 主容器
   - `PentestEnvSettings/RuntimeSection.tsx` — Java/Node/Ruby 运行时
   - `PentestEnvSettings/PythonEnvSection.tsx` — Python 环境
   - `PentestEnvSettings/shared.tsx` — 共享组件

3. **DashboardPanel.tsx (849 行) → 3 文件** ✅
   - `DashboardPanel/index.tsx` — 主容器 + 布局
   - `DashboardPanel/StatCards.tsx` — 统计卡片
   - `DashboardPanel/ActivityFeed.tsx` — 活动信息流

### 任务 3: 前端错误处理 (已评估 — 当前模式合理，不需要改)

---

## S 级架构优化计划 (2026-05 审计)

### 审计数据
- 后端: 32 crates, ~170K 行 Rust, 18 pub traits
- 前端: ~112K 行 TS/TSX, 650+ 文件
- 0 errors, 0 warnings (前后端均通过)
- 前端 API 层分离良好，组件零 invoke

---

### S-1: 拆分 golish 主 crate (进行中: 1/6 crate)

**已完成**:
- ✅ `golish-cli-output` (~2K 行) — 唯一不依赖 tauri/AppState 的模块

**分析发现**: 剩余 5 个提取目标全部深度耦合到 `tauri::State<AppState>`:
| 模块 | 行数 | tauri 耦合文件数 | 复杂度 |
|------|------|-----------------|--------|
| `indexer/` | 1681 | 需要分析 | 中 |
| `projects/` | 1386 | 需要分析 | 中 |
| `tools/vuln_intel/` | ~2K | 4/7 | 高 |
| `tools/scan_runner/` | ~2K | 5/6 | 高 |
| `tools/pipeline/` | ~3K | 8/13 | 高 |

**需要的额外架构工作**:
1. 将每个模块的 tauri command handlers 与纯业务逻辑分离
2. 纯逻辑移入新 crate，command handlers 保留在主 crate 作为薄包装
3. 通过 trait/struct 参数替代 `AppState` 直接访问

---

### S-2: 降低 golish-ai 耦合 (✅ 已完成: 11 依赖 → 7)

**已消除的 4 个依赖**:
- ✅ `golish-shell-exec`: 删除死代码 (`execute_shell_command_streaming` 未使用)
- ✅ `golish-web`: 定义 `WebFetchProvider` trait + `WebFetchResult` 在 golish-core，golish-web 实现
- ✅ `golish-skills`: 定义 `SkillProvider` trait + `SkillMetadata` 在 golish-core，golish-skills 实现 `DefaultSkillProvider`
- ✅ `golish-session`: 定义 `SessionManager` + `SessionManagerFactory` trait 在 golish-core，golish-session 实现

**当前 7 个依赖**: core, settings, tools, events, context, llm-providers, sub-agents

---

### S-3: 后端大文件拆分 (✅ 已完成)

| 文件 | 原行数 | 拆分结果 | 状态 |
|------|--------|---------|------|
| `golish-sub-agents/executor/inner.rs` | 990 | `inner.rs`(383) + `stream_processing.rs`(293) + `response_parsing.rs`(424) | ✅ |
| `golish-ai/db_traits.rs` | 778 | `mod.rs`(57) + `types.rs`(195) + `memory.rs`(128) + `tracking.rs`(202) + `repo.rs`(215) | ✅ |
| `golish/projects/file_storage.rs` | 749 | `mod.rs`(323) + `crud_ops.rs`(290) + `import_export.rs`(126) | ✅ |
| `golish-db/models/mod.rs` | 684 | `mod.rs`(11) + `session.rs`(303) + `pentest.rs`(240) + `wiki.rs`(132) | ✅ |
| `golish-ai/agentic_loop/mod.rs` | 671 | 已有 17 子模块，评估后不需进一步拆分 | ✅ 评估通过 |
| `golish-pentest/scanner.rs` | 670 | `scanner.rs`(155) + `scan_types.rs`(281) + `parsers.rs`(191) | ✅ |
| `golish-core/tool_name.rs` | 652 | 单一 enum+impls，结构清晰，评估后不需拆分 | ✅ 评估通过 |

---

### S-4: 前端大组件拆分 (8 个 >700 行)

| 组件 | 行数 | 拆分方案 |
|------|------|---------|
| `SubAgentSettings.tsx` | 842 | `SubAgentSettings/index.tsx` + `AgentEditor.tsx` + `ModelOverrides.tsx` |
| `AuditLogPanel.tsx` | 821 | `AuditLogPanel/index.tsx` + `LogTable.tsx` + `LogFilters.tsx` |
| `ContextPanel.tsx` | 811 | `ContextPanel/index.tsx` + `EventTimeline.tsx` + `SessionSummary.tsx` |
| `HomeView.tsx` | 779 | `HomeView/index.tsx` + `ProjectCards.tsx` + `QuickActions.tsx` |
| `FileEditorSidebarPanel.tsx` | 762 | `FileEditorSidebar/index.tsx` + `FileTree.tsx` + `EditorPane.tsx` |
| `PipelinePanel.tsx` | 761 | `PipelinePanel/index.tsx` + `StageCards.tsx` + `PipelineEditor.tsx` |
| `ProviderSettings.tsx` | 759 | `ProviderSettings/index.tsx` + `ProviderForm.tsx` + `ApiKeyInput.tsx` |
| `ScannerPanel.tsx` | 751 | `ScannerPanel/index.tsx` + `ScanConfig.tsx` + `ScanResults.tsx` |

---

### S-5: 前端类型迁移 (✅ 已完成)

**已替换的类型** (通过别名 re-export 保持向后兼容):
- ✅ `SessionMessageRole` → `GolishMessageRole`
- ✅ `SessionMessage` → `GolishSessionMessage`
- ✅ `SessionSnapshot` → `GolishSessionSnapshot`
- ✅ `SessionListingInfo`, `ReasoningEffort`, `TaskPlan`, `ApprovalDecision`, `ToolApprovalConfig` — 已在之前迁移
- ✅ 添加 `TextPart = Extract<PromptPart, { type: "text" }>` 辅助类型

**不替换的纯前端视图模型**: AiConfig, ProviderConfig, VertexAiConfig, OpenAiConfig, SubAgentInfo, WorkflowInfo, AgentFileInfo, SkillInfo, RuleInfo 等。

---

### 优先级排序

1. **S-5 类型迁移** — 最简单，立即可做，零风险
2. **S-4 前端组件拆分** — 中等难度，已有成功模式 (PentestEnvSettings/DashboardPanel)
3. **S-3 后端大文件拆分** — 中等难度，逐文件可做
4. **S-2 golish-ai 解耦** — 较大改动，需要设计 trait
5. **S-1 主 crate 拆分** — 最大工程，建议最后做

### 运行命令
```bash
# 重新生成 ts-rs 类型
just generate-types

# 验证前端编译
cd frontend && npx tsc --noEmit

# 验证后端编译  
cd backend && cargo check
```
