# 模块索引 · Module Index

> **这是给 AI agent 的导航入口。** 先读本索引看全貌，再按需打开具体模块卡，不要一次性吞整个代码库。每张卡用统一模板介绍一个模块：职责、公开接口、依赖关系、关键文件、坑、测试入口。
>
> 体系设计见 [`docs/design/2026-06-07-module-cards-system.md`](../design/2026-06-07-module-cards-system.md)。改任何模块都必须同步更新它的卡 + 本索引状态列（AGENTS.md §2.4 / §4 已强制）。

## 怎么用（给 AI）

1. 在下表按「一句话职责」定位相关模块
2. 打开对应「卡片」链接读细节
3. 卡内「何时该读这张卡」是触发提示；「依赖 / 被谁依赖」帮你判断改动影响面

## 图例

- 状态：✅ 已写卡 ｜ 🚧 进行中 ｜ ⬜ 待写
- 分层依据真实依赖图（`backend/crates/*/Cargo.toml` 内部依赖），从底层往上：基础 → 数据 → 执行/LLM → 工具 → 领域 → agent → app → 组合根。

---

## Backend（`backend/crates/`，共 57 crate）

### 基础层

| 模块 | 一句话职责 | 卡片 | 状态 |
|---|---|---|---|
| golish-platform | 跨平台抽象层，全仓库唯一允许写 cfg(target_os) 的地方 | [→](backend/golish-platform.md) | ✅ |
| golish-core | 最底层基础类型与 trait（Investigation五态policy、Hypothesis identity/verification/projection与whole-record comparison；Plan C closed catalogs与pending-admission authority） | [→](backend/golish-core.md) | ✅ |
| golish-settings | 集中式 TOML 配置（env 插值/原子写/类型安全 schema） | [→](backend/golish-settings.md) | ✅ |
| golish-events | AI 事件协调 + transcript/op_trace（含 Candidate Attempt terminal / Wave consolidation 安全摘要） | [→](backend/golish-events.md) | ✅ |
| golish-models | LLM 模型注册表与能力定义（metadata 取代字符串猜） | [→](backend/golish-models.md) | ✅ |
| golish-context | LLM 上下文窗口与 token 预算/压缩/截断 | [→](backend/golish-context.md) | ✅ |
| golish-cli-output | CLI 事件渲染（terminal/JSON/quiet 三模式） | [→](backend/golish-cli-output.md) | ✅ |
| golish-json-repair | 修复 LLM 畸形 JSON 工具参数，保证参数为 object | [→](backend/golish-json-repair.md) | ✅ |
| golish-udiff | 解析并应用 unified diff（多 hunk 外科式编辑） | [→](backend/golish-udiff.md) | ✅ |

### 数据 / 持久化

| 模块 | 一句话职责 | 卡片 | 状态 |
|---|---|---|---|
| golish-db | PostgreSQL持久化层（Tool Truth multi-root；Investigation freeze；Hypothesis Registry exact closure、atomic apply、projection/read worker；唯一00006） | [→](backend/golish-db.md) | ✅ |
| golish-graphiti | legacy GraphClient + scoped temporal node/edge Assertion-lineage graph（attested generation rebuild，旧 API 兼容） | [→](backend/golish-graphiti.md) | ✅ |
| golish-indexer | 代码索引基座（IndexerBackend + vtcode 后端 + git 工具） | [→](backend/golish-indexer.md) | ✅ |
| golish-artifacts | 自动维护项目文档提案（README/CLAUDE，未集成） | [→](backend/golish-artifacts.md) | ✅ |

### 执行 / Shell / LLM

| 模块 | 一句话职责 | 卡片 | 状态 |
|---|---|---|---|
| golish-pty | PTY/终端管理（会话/转义解析/渲染网格/shell 集成） | [→](backend/golish-pty.md) | ✅ |
| golish-shell-exec | shell 命令执行（PATH 继承 + 流式 + run_pty_cmd 工具） | [→](backend/golish-shell-exec.md) | ✅ |
| golish-sidecar | 后台被动捕获会话上下文（state.md/patches/artifacts） | [→](backend/golish-sidecar.md) | ✅ |
| golish-synthesis | LLM 生成 commit 消息/状态/会话标题 | [→](backend/golish-synthesis.md) | ✅ |
| golish-llm-providers | 统一 LLM provider 抽象（10+ provider） | [→](backend/golish-llm-providers.md) | ✅ |
| golish-prompts | prompt 组装系统（贡献者/registry/system prompt/摘要器） | [→](backend/golish-prompts.md) | ✅ |
| golish-session | AI 会话持久化（归档/双写文件+PG） | [→](backend/golish-session.md) | ✅ |
| golish-skills | Agent Skills 发现/解析/匹配（agentskills.io） | [→](backend/golish-skills.md) | ✅ |

### 工具 / 集成

| 模块 | 一句话职责 | 卡片 | 状态 |
|---|---|---|---|
| **golish-tools** | agent 工具执行系统（ToolRegistry + 文件/目录/shell/AST/联网/可信 Enumeration preflight schema） | [→](backend/golish-tools.md) | ✅ |
| golish-tools / file_ops | 文件读写增删改 5 工具，经沙箱校验 | [→](backend/golish-tools/file_ops.md) | ✅ |
| golish-tools / directory_ops | list_files / list_directory / grep_file | [→](backend/golish-tools/directory_ops.md) | ✅ |
| golish-tools / ast_grep | 结构化代码搜索与替换 | [→](backend/golish-tools/ast_grep.md) | ✅ |
| golish-tools / definitions | 工具 schema → LLM function declarations | [→](backend/golish-tools/definitions.md) | ✅ |
| golish-web | Web 搜索与内容抓取（Tavily/Brave + 抓取），封装成 agent 工具 | [→](backend/golish-web.md) | ✅ |
| golish-integrations | schema 驱动的外部服务凭据管理（FOFA/Quake/Hunter/Shodan/0.zone/ENScan/GitHub） | [→](backend/golish-integrations.md) | ✅ |
| golish-intel-providers | ASM/威胁情报 provider 抽象（0.zone/FOFA/Quake/Hunter/Shodan；typed DSL + exact-host mapping） | [→](backend/golish-intel-providers.md) | ✅ |
| golish-mcp | MCP 客户端集成（fail-closed 项目信任 + canonical builtin 来源、rmcp client、工具转换） | [→](backend/golish-mcp.md) | ✅ |
| golish-js-analyzer | JS bundle 静态分析（保留 callee/完整 receiver/byte span/相对 path 候选，兼容旧 Endpoint） | [→](backend/golish-js-analyzer.md) | ✅ |

### 领域（pentest / vuln / recon / scan）

| 模块 | 一句话职责 | 卡片 | 状态 |
|---|---|---|---|
| golish-pentest-domain | pentest 领域层（纯 Tool Truth contract/reducer、共享 confirmed-target Web Origin identity、I/O 边界 trait） | [→](backend/golish-pentest-domain.md) | ✅ |
| golish-pentest | pentest 工具执行引擎 + trusted-tool scoped output landing + multi-origin fingerprint observations + producer-org guarded 证据账本 | [→](backend/golish-pentest.md) | ✅ |
| golish-pentest-mcp | MCP server 二进制（把 pentest 工具作为 MCP 工具暴露） | [→](backend/golish-pentest-mcp.md) | ✅ |
| golish-vuln-intel-domain | 漏洞情报领域层（纯类型 + I/O 边界 trait，无 I/O 依赖） | [→](backend/golish-vuln-intel-domain.md) | ✅ |
| golish-vuln-intel | 漏洞情报引擎（NVD/CISA/RSS 摄取 + GitHub PoC + Nuclei 发现） | [→](backend/golish-vuln-intel.md) | ✅ |
| golish-scan-runner | guarded Recon runner（WhatWeb/ferox）+ current-owner exact-origin Nuclei 安全 template selector | [→](backend/golish-scan-runner.md) | ✅ |
| golish-projects | 项目配置存储 + `{project}/.golish/` 文件管理 + Unix dirfd / Windows capability-handle report storage（无 Tauri 依赖） | [→](backend/golish-projects.md) | ✅ |
| golish-memory-domain | Memory Fabric 纯领域契约（typed source/Episode/Assertion/event catalog + ContextPack values/layers + 1536 维 schema） | [→](backend/golish-memory-domain.md) | ✅ |
| golish-post-exploit-domain | Post-Exploit V2 纯领域契约（Foothold/Internal Asset/Attack Path/Action/Approval） | [→](backend/golish-post-exploit-domain.md) | ✅ |
| golish-cleanup-domain | Cleanup obligation/attempt/absence/waiver 纯状态机、exact frozen-scope CAS 与安全不变量 | [→](backend/golish-cleanup-domain.md) | ✅ |
| golish-reporting-domain | Canonical cited Report Read Model、typed EvidenceAudit/blocked-decision/sealed-handoff authority、双轴 revision 与 fail-closed validator | [→](backend/golish-reporting-domain.md) | ✅ |

### Agent

| 模块 | 一句话职责 | 卡片 | 状态 |
|---|---|---|---|
| golish-agent-kit | agent runtime底层构件（Plan B pure Candidate Gate、semantic-summary exactness、SQLx-free Registry/runtime ports；Stage Team receipt非truth） | [→](backend/golish-agent-kit.md) | ✅ |
| golish-agent-runtime | 高层agent runtime（operation-frozen dispatch、unified Investigation真实stage_run owner/progress、2–8 rolling live lanes、无latest/legacy fallback） | [→](backend/golish-agent-runtime.md) | ✅ |
| golish-agent-bridge | app↔runtime桥接层（stable request owner + closed HypothesisAnalysisStageRuntime Arc透传；不拥有rollout/Gate/canonical authority） | [→](backend/golish-agent-bridge.md) | ✅ |
| golish-sub-agents | sub-agent系统（含 dynamic Asset Verification exact actor binding + closed typed observation validator；receipt/prose非truth） | [→](backend/golish-sub-agents.md) | ✅ |

### App 层（Tauri command facades）

| 模块 | 一句话职责 | 卡片 | 状态 |
|---|---|---|---|
| golish-app-core | 应用边界共享类型（L5：GolishError/DbState/scoping/runtime + opaque trusted operator + exact trusted-target promotion + generation-guarded recon ports） | [→](backend/golish-app-core.md) | ✅ |
| golish-cleanup-app | Cleanup P7b exact terminal truth、backoff/fair DB-global worker、Gate/residual 与可恢复两阶段组织删除 | [→](backend/golish-cleanup-app.md) | ✅ |
| golish-agent-app | agent服务命令面（Plan B production runtime/finalizer composition + readonly Investigation IPC exact auth；并含既有Stage Team/Candidate/Memory/Cleanup/Reporting） | [→](backend/golish-agent-app.md) | ✅ |
| golish-pentest-app | pentest 服务命令面（operation-bound Enumeration producers + parameter-aware Nuclei DAST + 7-day adysec freshness/operation-pinned N-day proof + guarded EAS/Vuln producers + typed evidence landing） | [→](backend/golish-pentest-app.md) | ✅ |
| golish-recon-app | recon 服务命令面（含 TargetIntel fixed-endpoint pinned transport与 opaque raw-witness handoff；不拥有 canonical receipt DB） | [→](backend/golish-recon-app.md) | ✅ |
| golish-vuln-app | vuln-intel 服务命令面（feed/搜索/匹配/PoC·Nuclei 富化 + wiki） | [→](backend/golish-vuln-app.md) | ✅ |
| golish-platform-app | platform 服务命令面（vault/audit/notes/recordings） | [→](backend/golish-platform-app.md) | ✅ |
| golish-memory-app | Memory Fabric 应用服务（atomic canonical outbox + deterministic projectors + opaque-auth scoped ContextPack retrieval） | [→](backend/golish-memory-app.md) | ✅ |
| golish-post-exploit-app | Post-Exploit V2 app services（canonical facts + cleanup-bound P6b action/approval fence） | [→](backend/golish-post-exploit-app.md) | ✅ |
| golish-reporting-app | Reporting single-RR build/redaction + verified-artifact/outbox finalization protocol（无 RAG/KG authority） | [→](backend/golish-reporting-app.md) | ✅ |

### 组合根 + rig forks

| 模块 | 一句话职责 | 卡片 | 状态 |
|---|---|---|---|
| golish | 组合根 + Tauri桌面应用（DB-ready process-owned Investigation projector + readonly command facade + AppState/CLI lifecycle） | [→](backend/golish.md) | ✅ |
| golish / stage_run | headless 单/区间实跑 + shared-DB fork/recovery + controlled-fixture alias + Scoping→Reporting entity exact-set closure | [→](backend/golish/stage_run.md) | ✅ |
| rig-anthropic-vertex | rig fork：Claude on Vertex AI（CompletionModel + GCP 认证 + server tools） | [→](backend/rig-anthropic-vertex.md) | ✅ |
| rig-gemini-vertex | rig fork：Gemini on Vertex AI（CompletionModel + GCP 认证 + 流式） | [→](backend/rig-gemini-vertex.md) | ✅ |
| rig-openai-responses | rig fork：OpenAI Responses API（显式 reasoning 事件，o1/o3/gpt-5.x） | [→](backend/rig-openai-responses.md) | ✅ |
| rig-zai-sdk | rig fork：Z.AI GLM 原生 SDK（SSE 流式 + 伪 XML tool call 解析） | [→](backend/rig-zai-sdk.md) | ✅ |

## Frontend（`frontend/`）

| 模块 | 一句话职责 | 卡片 | 状态 |
|---|---|---|---|
| components | React UI组件（ChatPanel projection-only滚动恢复；unified Investigation exact route/sticky transcript；真实Agent树与server-owned control） | [→](frontend/components.md) | ✅ |
| hooks | React hooks（Tauri 事件订阅/终端/补全/主题/键盘等） | [→](frontend/hooks.md) | ✅ |
| lib | 非-UI基础设施（unified Investigation exact summary/list/detail/stop wrappers、opaque cursor/temporal snapshot与ts-rs closed wire） | [→](frontend/lib.md) | ✅ |
| pages | 独立页面（ComponentTestbed；主 shell 在 App.tsx） | [→](frontend/pages.md) | ✅ |
| services | 事件服务（ai-events 注册表 + Candidate/Reporting/Investigation refresh-only handlers + terminal-events） | [→](frontend/services.md) | ✅ |
| store | Zustand 全局 store（12 slice + selectors + types；Candidate/Reporting refresh hint；Investigation exact monotonic hint且无独立Pane route） | [→](frontend/store.md) | ✅ |
| styles | 终端/xterm 特化 CSS（通用走 Tailwind 4） | [→](frontend/styles.md) | ✅ |

---

## 进度

- 已写卡：**187**（52 backend crate 卡 + 128 目录子模块卡 + 7 前端子系统卡）／ 预计 ~187 — **全部 3 波完成** 🎉
- **Wave 1 完成** ✅：全部 52 个 backend crate 的 crate 卡。
- **Wave 2 完成** ✅：全部 backend crate 的目录子模块卡（**128 张**，每张实读 `mod.rs`/入口）。
  - 基础 15 · 数据 4 · 执行/LLM 17 · 工具/集成 12 · 领域 12 · agent 25（含 agent-kit 13）· app 17 · 组合根/rig 22（含 golish 17）· golish-tools 4（Wave 0）
- **Wave 3 完成** ✅：前端 7 个子系统卡（components/hooks/lib/pages/services/store/styles），均实读 `frontend/` 真实结构 + 入口文件。
- 一致性：0 个 ⬜ 残留（backend + frontend 全 ✅）；卡间链接 0 broken。后续维护见 AGENTS.md §2.4/§4（改模块同步更新卡 + 本索引）。

## Card synchronization notes

- `golish-agent-runtime/agentic_loop`, `golish-agent-kit/db_traits`, `golish-agent-app/ai`, and `golish-db/repo` are current as of 2026-08-14 for atomic Investigation Generator recovery: exact finished Primary submissions are consumed before model redispatch, fresh members and Generator authority materialize in one transaction, orphan members require one earliest exact canonical source, and deterministic repository authority/infrastructure failures halt the current outer request. Statuses remain ✅ pending the coordinated focused runtime/DB verification recorded by the owning tasks.
- `golish-db/repo` and `golish-agent-runtime/agentic_loop` are current as of 2026-08-14 for exact closed post-synthesis Asset Primary recovery: startup preserves the witnessed expired same-chain shell, claim re-leases it in place, and runtime replays its unique typed synthesis through census/task-plan sealing and compiler admission without another model call. Statuses remain ✅ pending focused migrated/runtime verification.
- `golish/stage_run` and `golish-agent-runtime/agentic_loop` are current as of 2026-08-14 for full Asset Primary execution-lineage chain ownership and fresh infrastructure-shell bound-chain reload: Primary agent classification requires exact Worker/WorkItem lineage membership, while a generation-N empty shell reloads only its exact persisted Primary chain. Statuses remain ✅ pending focused verification.
- `golish-agent-runtime/agentic_loop`, `golish-agent-kit/db_traits`, `golish-agent-app/ai`, and `golish-db/repo` are current as of 2026-08-14 for schedule-round-invariant Investigation Analysis identity: each asset evolution uses the legacy v1 Primary formula only as a logical anchor, appends a distinct `asset-analysis-work-v2` physical work identity, and resolves successor pending-evolution authority through one exact server selector rather than caller `None` or latest/global state. Statuses remain ✅.
- `golish-agent-runtime/agentic_loop`, `golish-agent-app/ai`, and `golish-db/repo` are current as of 2026-08-14 for the fresh discovery loop: canonical predicate arguments are objects end-to-end, terminal semantic duplicates are audit-only during reanalysis, and each new Verification round binds the Asset Primary authority at the lane's exact current evolution epoch. A production-binary stage fork proved discovery→admission→successor root→resolution→queue close→Gate PASS. Statuses remain ✅.
- `golish-app-core/ports`, `golish-agent-kit/db_traits`, `golish-agent-kit/tool_executors`, `golish-agent-app/ai`, and `golish-agent-runtime/agentic_loop` are current as of 2026-08-12 for cognition exact reads: wiki search/read share one normalized indexed-page path with a guarded bundled fallback, while Investigation Analysis Primary receives complete host-frozen redacted authority bodies and children receive only their exact evidence-selected subset; empty selectors fail closed and all projected data remains non-instructional. Statuses remain ✅.
- `frontend/components` is current as of 2026-08-11 for Chat/detail scroll continuity: projection-only ChatPanel remounts restore per-conversation bottom/history intent and rebind observers, while unified Investigation reuses actor-keyed transcript stickiness without overriding deliberate upward reading. Status remains ✅.
- `golish-agent-runtime/agentic_loop` and `frontend/components` are current as of 2026-08-11 for unified Investigation detail identity: outer `call_...` owns card selection/progress display, `stage_run:<execution>` owns the durable run, and terminal panes map to their exact conversation `aiSessionId` before IPC. Conflicting identities, missing mappings and latest fallbacks remain fail closed. Statuses remain ✅.
- `frontend/components` is current as of 2026-08-11 for Investigation dual-session rendering: the owning `aiSessionId` remains the backend authority while the pane session owns live transcript/refresh projections, and absent Zustand collections use a stable snapshot sentinel so React 19 cannot enter an external-store update loop. Status remains ✅.
- `golish-db/repo` is current as of 2026-08-11 for retained Investigation detail reads: the exact Main actor is the unified `investigation_primary/leader:primary` server seed under the sealed root scope, never a generic `company_stage_controller`; Registry authority accepts the Candidate writer's complete five-member unavailable-feed census while partial negative censuses remain fail closed. Status remains ✅.
- `golish-agent-app/ai` is current as of 2026-08-11 for restart-safe Investigation reads: finished materialized projections remain readable after their in-memory bridge is gone, while nonterminal reads and every control/mutation still require the live exact-workspace bridge. Status remains ✅.
- `golish-db/repo` and `golish-agent-app/ai` are current as of 2026-08-12 for terminal Investigation audit reads: an exact closed/abandoned run whose terminal head event predates authority expiry reopens at that immutable event as `temporally_stale`; active, malformed and post-expiry terminal runs remain fail closed, and historical authority never enables control/JIT. Statuses remain ✅.
- `golish-db/repo` is current as of 2026-08-12 for retained Investigation compiler Hypothesis reads: the reader accepts the compiler's exact frozen production body through a separate closed typed shape, validates proof authority/source roles, derives at-time subject identity only from the same immutable semantic key, and binds stage-run list/detail scope joins to the selector snapshot. Status remains ✅.
- `golish-agent-app/ai`, `golish-agent-kit/db_traits`, and `golish-db/repo` are current as of 2026-08-12 for Verification material-FactDelta evolution authority: support/contradiction waves atomically persist exact consumptions plus an append-only pending authority instead of a fixed-point receipt; scheduler state exposes pending IDs/counts and remains nonterminal until canonical successor apply finalizes the batch as advanced. Statuses remain ✅.
- `golish-agent-app/ai`, `golish-agent-kit/db_traits`, and `golish-db/repo` are current as of 2026-08-12 for evolution compiler finalization and response-loss recovery: new semantic revisions atomically finalize pending batches as advanced; zero/AttachCurrent-only results preserve compiler audit and close the source generation as fixed point without an empty successor; exact stable-request recovery returns a typed `evolution_fixed_point` terminal while foreign prepared authority fails closed. Statuses remain ✅.
- `golish-agent-app/ai`, `golish-agent-kit/db_traits`, `golish-db`, and `golish-db/repo` are current as of 2026-08-12 for durable successor-Analysis rearm: one DB-owned fingerprint binds the pending authority to the latest sealed Candidate ordinal-zero attempt; an append-only receipt reopens the StageTeam and deterministically creates an Evolution Analysis Primary with the retained durable chain, exact replay, coordinator child/park/complete authorization, and multi-round Analysis cursor recognition. Statuses remain ✅.
- `frontend/components` is current as of 2026-08-12 for Investigation sequence domains: projection change-seq binds read pagination/refresh, run-control change-seq binds Stop CAS with the run-state head, and the two valid sequences are never compared as identity. Status remains ✅.
- `golish-pentest-app/pentest_bridge`, `golish-pentest`, `golish-pentest/output_store`, `golish-db/repo`, `golish-agent-kit/task_orchestrator`, and `golish-sub-agents/defaults` are current as of 2026-07-20 for adaptive EAS service fingerprinting: exact DB pending-port plans, per-IP bounded Nmap XML chunks, fixed concurrency, slow-target isolation, one recovery pass, strong/weak attempt separation, backend-owned deadlines, and tunnel-aware HTTPS exact-origin materialization for Nmap `http + ssl/tls` results. Statuses remain ✅.
- `golish-agent-kit/task_orchestrator`, `golish/cli`, and `golish/stage_run` are current as of 2026-07-18 for Scoping-only routine human confirmation: typed target-scope and Candidate review barriers remain fail-closed, while later ordinary Gate-PASS stage crossings auto-advance without a generic phase confirmation card; the CLI phase-boundary flag remains accepted only for compatibility. Statuses remain ✅.
- `golish-agent-runtime/agentic_loop` is current as of 2026-07-18 for Stage Team child evidence-manifest landing recovery: exact unknown/foreign evidence authority failures discard the invalid submission and requeue the stable WorkItem within its frozen attempt budget, while lease/fence/version/storage/internal-hash failures remain fail-closed system errors. Status remains ✅.
- Historical 2026-07-18 plan-first V2 Company-stage reset used exact active-tool unknown-outcome closure; its reset-time active-tool behavior is superseded by the 2026-07-20 fail-before-mutation protocol below. Narrow reset-marker resume admission and target-snapshot-first organization hard-delete convergence remain current. Statuses remain ✅.

- `frontend/hooks`, `frontend/lib`, and `frontend/components` are current as of 2026-07-17 for restore-window AI event buffering and DB-authoritative recovery of Company Controller dispatch cards whose historical parent tool event was lost; recovered cards are inserted at the durable Request time instead of remaining a timeline footer. Statuses remain ✅.

- `frontend/components` is current as of 2026-07-14 for persistent AIChatPanel context-compaction visibility; its module status remains unchanged.
- `frontend/components` is current as of 2026-07-15 for fail-closed AIChatPanel execution-profile commit/send/restore semantics; its module status remains ✅.
- `golish-db/repo`, `golish-agent-runtime/agentic_loop`, `golish-agent-app/ai`, `frontend/lib`, and `frontend/components` are current as of 2026-07-14 for durable Stage Team repair/operator recovery and the exact Candidate Verification/pending-enrichment queue; `golish-db` additionally records the 2026-07-15 audited checksum/forward-migration recovery contract; all module-card statuses remain ✅.
- `golish-agent-app/ai` and `golish-agent-kit/task_orchestrator` are current as of 2026-07-14 for unit-less coordinator pass-token normalization and DB-authoritative fan-out closeout; statuses remain ✅.
- `golish-agent-app/ai`, `golish-agent-kit/task_orchestrator`, and `golish/stage_run` are current as of 2026-07-15 for the typed fresh-target authority tri-state, explicit CLI `--org` confirmed-identity fast path (target-empty and held before historical-target DB reads), deferred V2 Scoping finalization before `stage_passed` with deterministic replay/identity checks, headless exact-resume missing-marker fail-closed behavior, GUI prompt-only `UnconfirmedSubject`, and current-invocation exact-target shape validation; statuses remain ✅.
- `golish-db/repo` and `golish/stage_run` are current as of 2026-08-11 for Human-confirmed Company Identity finalization and exact pre-freeze Scoping resume: a missing second resolver call is recovered only from the same operation/execution's immutable candidate set, terminal Human choice and later root create; arbitrary expected-org selectors remain fail closed. Statuses remain ✅.
- `golish-agent-app/ai`, `golish/cli`, and `frontend/components` are current as of 2026-07-15 for GUI/CLI shared provider and context-config normalization; statuses remain ✅.
- `golish-agent-app/ai` is current as of 2026-07-15 for fresh GUI/CLI launch profile validation; unknown embedded profiles fail closed before operation creation instead of silently selecting another DAG.
- `golish/stage_run` is current as of 2026-07-15 for typed phase-boundary approval parity: headless confirmation stays fail closed unless the invocation explicitly combines `--auto-approve` with `--approve-phase-boundaries`, the CLI equivalent of the GUI Confirm card.
- `golish-agent-bridge/agent_bridge` is current as of 2026-07-15 for the GUI Task success-path stack boundary: completed primary turns bypass the provider failover future, while real primary errors retain the existing fallback policy; status remains ✅.
- `golish-agent-runtime/agentic_loop` and `frontend/components` are current as of 2026-07-15 for Company Controller child-output protocol/dependency retry semantics, exact-pointer legacy/Team mutual exclusion, and the compact output-authoritative Team status view; statuses remain ✅.
- `frontend/components` is current as of 2026-07-15 for Target delete convergence: single/bulk deletes update local state immediately, stale target-list responses cannot restore removed rows, all destructive entries use an in-app confirmation dialog, and two-phase organization deletion waits for the durable row removal before refresh; status remains ✅.
- `golish-db/repo`, `golish-agent-runtime/agentic_loop`, `frontend/lib`, and `frontend/components` are current as of 2026-07-15 for Stage Team WorkerRun UI isolation, ordered reasoning flush/timing, transient tool-fence transaction retry, and narrow local-provider split-state recovery; statuses remain ✅.
- `golish-db/repo`, `golish-agent-runtime/agentic_loop`, `golish-agent-kit/tool_executors`, `golish-sub-agents/defaults`, and `frontend/components` are current as of 2026-07-15 for controller-only Stage Team seeding, Codex-compatible chain-local `update_plan`, composite parallel plan steps, exact expired-Controller reclaim, preview/submit `reason_kind` parity, same-Controller final submission/Gate repair continuity, and a single current Controller plan card whose failed/superseded raw updates remain diagnostic-only; statuses remain ✅.
- `golish-db/repo`, `golish-agent-runtime/agentic_loop`, `golish-sub-agents/executor`, and `golish/stage_run` are current as of 2026-07-15 for structured Controller chain authority, claimed-Unit refresh, router-only coordination barriers, operation-fresh aggregate closeout claims, and terminal-aware CLI/run-tree execution selection; statuses remain ✅.
- `golish-agent-app/ai` is current as of 2026-07-15 for AttackCandidate submit-preview evidence membership against the exact frozen work item manifest; status remains ✅.
- `golish-agent-kit/task_orchestrator`, `golish-agent-app/ai`, and `frontend/components` are current as of 2026-07-16 for the operation-bound TargetIntel→EAS exact target review: one explicit non-empty subset confirmation replaces the duplicate generic active-scan approval, while direct EAS and provider-derived authority remain fail closed; statuses remain ✅.
- `golish-agent-runtime/agentic_loop` and `frontend/components` are current as of 2026-07-16 for terminal Company Controller progress convergence: new runs emit exact passed/blocked Unit progress, while successful historical `stage_run` results repair stale request-scoped running summaries during replay; statuses remain ✅.
- `golish-db/repo`, `golish-agent-runtime/agentic_loop`, and `golish-agent-kit/task_orchestrator` are current as of 2026-07-16 for interrupted EAS wrapper same-chain reconciliation and Gate-blocked UI semantics: bounded exact-scope wrappers resume from durable worklist gaps, while blocked stages no longer emit `SubtaskCompleted` or promise unconditional recovery; statuses remain ✅.
- `golish-agent-kit/harness`, `golish-agent-runtime/agentic_loop`, `golish-agent-app/ai`, `golish-pentest-app/pentest_bridge`, `golish/stage_run`, and `frontend/components` are current as of 2026-07-16 for downstream Company Controller convergence: Target Intel/EAS/Enumeration/Vuln share one durable Team contract with frozen stage specialists and DB-backed UI; Controller park/heartbeat, one-round no-migration Gate repair, anonymous eligible-id recovery, and legacy telemetry role folding are documented; statuses remain ✅.
- `golish-agent-runtime/agentic_loop`, `golish/stage_run`, and `frontend/components` are current as of 2026-07-16 for company-stage legacy retirement: non-V2 operations are typed rerun-required, generic specialist fallback and legacy collector cards are unavailable, exact resume accepts validated Team children while selecting the unique Controller, and Candidate/Verification/later typed views remain separate; statuses remain ✅.
- `frontend/components` is current as of 2026-07-16 for Codex-style Company Controller drill-in: exact `stage_team_dispatch_workers` children replace the generic tool row with clickable SubAgent cards, retain stack-based back navigation, and keep the Controller header live while delegated children run; status remains ✅.
- `frontend/components` is current as of 2026-07-16 for passed-stage Controller plan convergence: exact Team lead identities resolve the matching organization row, and only server-authored `passed` truth projects a stale final plan snapshot to display-complete; blocked/error/stopped states remain unfinished; status remains ✅.
- `golish-agent-runtime/agentic_loop` and `frontend/components` are current as of 2026-07-16 for Stage Team dispatch fan-out identity: every accepted WorkerRun derives a distinct `${dispatch_tool_request_id}::worker:<worker_run_id>` transcript/UI key, while the Controller immediately shows one numbered assignment card per dispatch request and keeps not-yet-started children visible as queued; statuses remain ✅.
- `golish-db/repo` and `golish-agent-app/ai` are current as of 2026-07-16 for Codex-style Thread/Turn continuation: a stable Task/Operation appends durable `operation_turns` from either `running` or `waiting`, while expired clean Controller/child workers resume the same WorkItem, WorkerRun and message chain with an incremented attempt epoch; outcome-unknown active tools remain recovery-required. Existing worker-keyed frontend cards therefore continue on one timeline without a new IPC/UI branch; statuses remain ✅.
- `golish-db/repo` and `golish-agent-app/ai` are current as of 2026-07-16 for exact Company Controller chain ownership: `company_stage_controller` is a server-owned `pentester` at both persistence and resume validation boundaries, so a valid same-session/same-operation Controller chain is not rejected as cross-owned; unknown roles and real ownership drift remain fail closed. Statuses remain ✅.
- `golish-db/repo` is current as of 2026-07-16 for Stage Team dynamic-child replay canonicalization: accepted child creation and replay validation share the same objective/hash/ref material, including the Controller assignment envelope, so a continued operation validates existing children without rewriting them. Status remains ✅.
- `golish-agent-runtime/agentic_loop` is current as of 2026-07-16 for Company Controller operator-recovery propagation: a parked Controller with an outcome-unknown child returns a typed DB-backed recovery blocker and closes same-request reentry instead of reporting a generic failed Controller or falling back to direct tools. Status remains ✅.
- `golish-agent-runtime/agentic_loop` is current as of 2026-07-16 for terminal Stage Run request control: closed operator-recovery/reentry ToolResults become a typed dispatch halt, suppress later same-batch calls, and end the current model loop after result persistence without changing successful scheduler-read semantics. Status remains ✅.
- `golish-sub-agents/executor`, `golish-agent-runtime/agentic_loop`, and `frontend/components` are current as of 2026-07-16 for Stage Team child completion/retry truth: ordinary children receive a typed `stage_worker_output.v1` barrier, invalid output corrects the exact failed WorkerRun UI state before retry, and retry generations of one WorkItem render as one assignment with attempt history rather than fake sibling cards; statuses remain ✅.
- `golish-db/repo` and `golish-agent-runtime/agentic_loop` are current as of 2026-07-16 for reconciled-child CLI continuation closure: seed replay preserves historical WorkItem epochs while validating dynamic/gate-repair authority; ordinary Company Controller Gate BLOCK and aggregate-token unavailability close only the current request; an authorized successor-Turn authority atomically restores the same plan/unit/leader/Controller WorkerRun/message chain without replacing the Agent or rewriting historical gaps. Migration-time legacy no-gap witnesses are immutable and post-migration creation remains forbidden; statuses remain ✅.
- `golish-db` is current as of 2026-07-16 for the Operation Turn migration restart hotfix: checksum repair accepts only the audited `20260716000001` trailing-blank-line old→new SHA-384 pair via metadata CAS, while every other unknown/dirty drift remains fail closed; real CLI and GUI cold starts both reached DB ready and Target data readback. Status remains ✅.
- `frontend/store`, `frontend/lib`, and `frontend/components` are current as of 2026-07-16 for same-identity durable child resume display: cold restore marks stale Agent/tool work interrupted, the authoritative repeated `sub_agent_started` revives the exact WorkerRun card while preserving history, and every detail surface leaves old tools terminal while only newly issued tools render running. Statuses remain ✅.
- `golish-db`, `golish-db/repo`, `golish-agent-runtime/agentic_loop`, `golish-pentest-app`, and `frontend/components` are current as of 2026-07-17 for exact-gap Controller successor repair, independent two-Turn resume fuel, typed WhatWeb ledger status in Target Surface, and the rule that frontend status never pre-authorizes Enumeration exclusion. Statuses remain ✅.
- `golish-agent-kit`, `golish-agent-kit/harness`, `golish-agent-runtime/agentic_loop`, `golish-db`, `golish-db/repo`, and `golish-agent-app/ai` are current as of 2026-07-17 for cardinality-safe Vuln `TechniqueOutcomeSet` final seals, fixed-window replay, complete Reporting expansion, post-aggregation Candidate fuel, and post-Gate finalization failure routing. Statuses remain ✅.
- `golish-pentest-app/pentest_bridge` is current as of 2026-07-17 for Enumeration JS extraction positive-truth precedence: persisted API/parameter findings remain evidence-backed terminal `found` even when unresolved diagnostics coexist, while zero-fact incomplete extraction stays `partial` and terminal sibling truth suppresses identical no-progress repair. Status remains ✅.
- `golish-agent-kit`, `golish-agent-app/ai`, `golish-agent-runtime/agentic_loop`, and `golish-db/repo` are current as of 2026-07-17 for Scoping pre-freeze subsidiary evidence authorization, evidence-backed checked-empty results, current `{rows:[]}` unit-review compatibility, and latest same-root human-choice repair convergence. Statuses remain ✅.
- `golish-sub-agents/executor`, `golish-agent-runtime/agentic_loop`, and `frontend/components` are current as of 2026-07-17 for canonical target-scoped Stage Team dispatch: model-visible refs require `kind + target_id`, safe historical target shorthand is canonicalized and deduplicated before DB reauthorization, durable rejection decisions survive ToolResult routing, and historical all-rejected assignments render error rather than queued. Statuses remain ✅.
- `golish-agent-kit/harness`, `golish-agent-runtime/agentic_loop`, `golish-db/repo`, and `frontend/components` are current as of 2026-07-17 for scope/epoch-bounded Company Controller worker admission: frozen lifetime totals remain replay metadata only, C/G/K live concurrency and per-WorkItem attempts remain enforced, and the UI no longer presents a lifetime worker quota. Statuses remain ✅.
- `golish-agent-kit/harness` is current as of 2026-08-02 for the schema-neutral unified Investigation topology foundation: the closed stage catalog includes Application Understanding and Investigation; server-owned legacy/unified topology values have stable canonical hashes; the legacy graph and all current production profiles remain unchanged, while the separate unified graph fixes `Vuln → AU → Investigation → Reporting`. Runtime/DB/profile cutover remains pending. Status remains ✅.
- `golish-pentest-app/pentest_bridge` and `golish-db/repo` are current as of 2026-07-17 for zero-body uniform-baseline filtering and exact-origin Candidate directory-set aggregation with complete count/hash plus bounded deterministic preview; the final 100-item policy remains post-aggregation. Statuses remain ✅.
- `golish-pentest-app/pentest_bridge` is current as of 2026-07-17 for the local Nuclei HTTP/SSL template policy: proof, active Vuln scans, and exact replay share one canonical local tree without the broken `-dut` filter, while code/JavaScript/headless/file/OAST/remote-template capabilities remain unavailable and stage/scope/exact-origin/tag-or-exact-id/rate guards remain mandatory. Status remains ✅.
- `golish-pentest-app/pentest_bridge` is current as of 2026-07-18 for Nuclei operation template snapshots: General binds only operator HTTP/SSL or DAST subtrees and never adysec; fingerprint-targeted checks remote adysec freshness at most once every seven days and pins one commit per Vuln operation; offline proof is reused by operation/source/selection and snapshot-scoped failure stops sibling URL retries without becoming checked-empty. Status remains ✅.
- `frontend/components` is current as of 2026-07-17 for request-scoped Attack Candidate `stage_run` detail routing: live/blocked/completed Attack Analyst rows remain visible and drillable even when production args contain only `orgs`, while explicit stage conflicts fail closed and Candidate Review remains DB-backed. Status remains ✅.
- `golish-db`, `golish-db/repo`, `golish-agent-runtime`, `golish-agent-runtime/agentic_loop`, and `frontend/components` are current as of 2026-07-17 for repair-epoch dispatch convergence: a stable Company Controller may parent current-epoch children only under exact DB-owned repair/successor authority, while terminal persistence failures render error instead of permanent queued cards. Statuses remain ✅.
- `golish-agent-runtime/agentic_loop` is current as of 2026-07-17 for closed final-submitter continuation: a Controller that prepared finalization but returned without a durable submission is resumed through the exact Aggregator claim on the same WorkItem/WorkerRun/message chain instead of being rejected by the open-leader fence. Status remains ✅.
- `golish-sub-agents/executor`, `golish-agent-runtime/agentic_loop`, and `golish-db/repo` are current as of 2026-07-18 for Stage Team overlap/fence/drain convergence: exact duplicate normalized assignments are rejected before durable writes, begin/finish tool fences share bounded `40P01`/`40001` transaction retry, only typed begin `LeaseLost` poisons the shared bound lease across both lifecycle and generic wrapper, and one child error no longer skips claimable sibling retries. Statuses remain ✅.
- `golish/cli`, `golish/stage_run`, `golish-agent-app/ai`, `golish-agent-kit/task_orchestrator`, `golish-agent-runtime/agentic_loop`, `golish-db`, and `golish-db/repo` are current as of 2026-07-18 for shared-DB immutable-source stage forks: Scoping adopts sealed scope truth, post-Scoping predecessors use exact per-org final seals, active-stage Targets are snapshotted/fenced, and Candidate-only uses the existing Wave/Gate/Review kernel through a narrow fork entry. Statuses remain ✅.
- `golish-sub-agents/executor` is current as of 2026-07-19 for Vuln Nuclei timeout ownership: the two guarded Nuclei wrappers retain control through their server-validated 10–600 second foreground deadline and DB landing instead of being dropped by the SubAgent 300-second fallback; generic and anonymous-access tools keep the outer timeout, and timeout remains non-terminal coverage. Status remains ✅.
- `golish-agent-runtime`, `golish-agent-runtime/agentic_loop`, `golish-sub-agents/executor`, `golish-core`, `golish-app-core`, `golish-pentest-app/pentest_bridge`, `golish-agent-app/ai`, `frontend/lib`, and `frontend/components` are current as of 2026-07-19 for the Vuln DB-authoritative worklist executor: exact-origin capability shards replace LLM formulaic fan-out, primary budget failure narrows to finite single-technique retries, Nuclei cancellation is kill+await+landing, stable target transport can become evidence-backed blocked while runtime failure remains partial, and UI exposes terminal/total/remaining plus separate historical/retry/recovery lanes. Statuses remain ✅; real CLI acceptance is intentionally pending.
- `golish-db`, `golish-db/repo`, `golish-cleanup-domain`, `golish-cleanup-app`, `golish-app-core`, `golish-recon-app/organizations`, `frontend/lib`, and `frontend/components` are current as of 2026-07-19 for organization-deletion/stage-fork admission convergence: both writers share live Organization lock ordering, reject the losing race before partial cleanup/fork materialization, preserve a typed blocker through IPC, and stop frontend timeout polling on admission rejection. Statuses remain ✅.
- `golish-db/repo` and `golish-cleanup-app` are current as of 2026-08-11 for retained-authority organization deletion: live Organization/Target rows are physically removed while sealed Tool Truth, Enumeration, Hypothesis, Reporting, bound audit rows and append-only target UUIDs remain unchanged; future writes retain key-share live-parent/scope/deletion admission, and raw unprojected endpoint/origin rows are explicitly purged. Statuses remain ✅.
- `golish-db`, `golish-db/repo`, `frontend/lib`, and `frontend/components` are current as of 2026-07-19 for quiescent organization-deletion convergence: a waiting stage Task with no live Worker lease or active tool pointer is atomically closed and audited inside deletion admission, while durable executor authority or unresolved tool ownership still returns the typed blocker and the confirmation copy explains both paths. Statuses remain ✅.
- `golish-db/repo`, `golish-agent-app/ai`, and `golish-memory-app` are current as of 2026-07-20 for Candidate Gate closure and Memory Fabric health: contextual Candidate targets remain anchored to exact frozen work items while live pointers prove fork scope, bound Workers receive scoped ContextPack, Target Intel empty final seals receive one replayable server attestation, and loopback Ollama supplies both 1536-dimensional projection and VectorPrior query embeddings. Statuses remain ✅.
- `golish-db` is current as of 2026-07-20 for Enumeration manifest migration recovery: the exact locally applied `20260717000003` SHA-384 may advance only to the catalog-audited current SHA-384 through compare-and-swap; all other checksum, description, dirty-row or schema-history drift remains fail closed. Status remains ✅.
- `frontend/components` is current as of 2026-07-20 for Vuln scheduler presentation: DB-owned formulaic WorkItems render as scan shards with evidence coverage and retry/recovery lanes, never as fake SubAgents, while the raw scheduler-details affordance is absent from the Vuln product view. Status remains ✅.
- `golish-agent-runtime` and `golish-agent-runtime/agentic_loop` are current as of 2026-07-20 for shared Stage Team rolling child drain: Target Intel, EAS, Enumeration, and Vuln refill an authorized child slot as soon as any sibling finishes, while frozen company/provider/child caps, durable claim fences, cancellation landing, evidence, and Gate authority remain unchanged. Statuses remain ✅.
- `golish-agent-app`, `golish-agent-app/ai`, `golish-db`, `golish-db/repo`, `frontend/components`, `frontend/lib`, and `frontend/store` are current as of 2026-07-20 for ChatPanel Company-stage full-reset consistency: only four Company stages may reset in place; sealed frozen scope, overlapping-active-operation rejection, operation/session run ownership, active external-tool rejection, modern EAS/Enumeration fact purge, retained Target/Vuln history, and runtime/facts/state/graph/cursor atomicity are backend authority, while the frontend reconciles memory+persistence suffixes onto one existing roadmap owner and serializes reset with send/mode/conversation controls. Statuses remain ✅.
- `golish-agent-runtime`, `golish-agent-runtime/agentic_loop`, `golish-pentest-app`, and `golish-pentest-app/pentest_bridge` are current as of 2026-07-20 for Vuln scan-budget terminalization: primary/narrowed/budget-recovery use 300/600/600-second server budgets, an old exact attempt-3 budget exhaustion receives one real attempt-4 recovery, and only a witnessed foreground maximum-budget deadline may become evidence-backed blocked; plain exit 124 and other scanner/runtime failures remain partial. Statuses remain ✅; same-operation live acceptance is pending.
- `golish-agent-runtime` and `golish-agent-runtime/agentic_loop` are current as of 2026-07-20 for Vuln final-seal structural N/A materialization: exact Enumeration-manifest N/A is re-anchored through one fresh current-operation/org/Unit/session/project aggregate evidence row before operation-scoped outcome upsert; model N/A, forged snapshot authority, stale predecessor evidence, scope drift and zero/failed evidence append remain fail-closed, while the full terminal-coverage equality check is unchanged. Statuses remain ✅; same-operation final-seal acceptance is pending.
- `golish-db`, `golish-db/repo`, `golish-agent-kit`, `golish-agent-kit/db_traits`, `golish-agent-app`, `golish-agent-app/ai`, `golish-agent-runtime`, and `golish-agent-runtime/agentic_loop` are current as of 2026-07-20 for Company Controller finalizer restart recovery: deterministic closeout failure parks the exact durable final submitter without producer fuel, startup requeues exact-submission finalizers, and already-immutable failed/exhausted history receives a witness-bound no-purge same-stage runtime rollover plus explicit current-request halt. Statuses remain ✅; Test1 live continuation is pending.
- `golish-agent-kit/harness`, `golish-agent-app/ai`, `golish-agent-runtime/agentic_loop`, `golish-sub-agents/defaults`, `golish-pentest-app/pentest_bridge`, `golish-db/repo`, and `golish-pentest/output_store` are current as of 2026-07-21 for AI-owned TLS Candidate decisions with evidence/reason closure, frozen-manifest Nuclei template selectors, pre-final-seal duplicate Candidate identity repair, and server-owned quick/standard/full TCP coverage with read-side Gate attestation, per-host 65535 accounting, no-network wide-CIDR policy closure, monotonic terminal truth, `-Pn` liveness isolation, and IP+port endpoint results. Statuses remain ✅.
- `golish-app-core`, `golish-shell-exec`, `golish-pentest-app/pentest_ai`, `golish-pentest-app/pentest_bridge`, `golish-agent-app/ai`, `golish-agent-runtime/agentic_loop`, `golish-sub-agents/defaults`, `golish-sub-agents/executor`, `frontend/services`, `frontend/store`, `frontend/lib`, and `frontend/components` are current as of 2026-07-23 for Codex-style same-session managed processes: elapsed time never kills a managed child; adjustable initial/read yield returns the same live handle and output activity; main/sub-agent shell aliases share one manager; shell/pentest roles receive check/kill controls and treat quiet as observation only; typed reconcilers land EAS authority/evidence before completion; Naabu v3 uses fixed Connect recipes and full-output spooling; Gate independently validates v3 while retaining strict v1/v2 reads; UI has no hard-deadline countdown. Statuses remain ✅.
- `golish-db/repo`, `golish-agent-kit/db_traits`, `golish-agent-app/ai`, `golish-agent-runtime`, `golish-agent-runtime/agentic_loop`, `golish/cli`, and `golish/stage_run` are current as of 2026-07-20 for the completed Vuln no-purge Gate closure: source-epoch terminal adoption, monotonic terminal outcomes, array-safe finalizer checkpoints, exact-origin structural N/A materialization, and a prefix-guarded cloned-database CLI selector produced a fresh 180/180 Gate PASS while leaving the production DB unchanged. Statuses remain ✅.
- `golish-app-core`, `golish-agent-app/ai`, `golish-sub-agents/executor`, `frontend/services`, `frontend/store`, and `frontend/components` are current as of 2026-07-21 for runtime-owned background-job reconciliation: terminal-unreconciled retention, exactly-once lag replay, one-invocation event waiting, recovery-only control tools, exact-origin global navigation, and persistent background lifecycle Detail panels. Statuses remain ✅.
- `golish-agent-runtime` and `golish-agent-runtime/agentic_loop` are current as of 2026-07-21 for rolling-refill lock convergence: child completions remain pollable while the single durable refill claim waits on the same operation row lock, `None` pauses until child progress, and all existing concurrency, cancellation landing, evidence and Gate boundaries remain unchanged. Statuses remain ✅.
- `golish-agent-runtime` and `golish-agent-runtime/agentic_loop` are current as of 2026-07-21 for parked-Controller queued continuation: unfinished required producer WorkItems enter the rolling drain even when no WorkerRun exists yet, so restart resume can claim durable queued shards instead of misreporting `NoRunnableChild`; operator recovery priority and claim authority remain unchanged. Statuses remain ✅.
- `golish-pentest-domain`, `golish-db`, `golish-db/repo`, `golish-pentest-app/pentest_bridge`, `golish-agent-kit/harness`, `golish-agent-kit/db_traits`, `golish-agent-app/ai`, `golish-agent-runtime/agentic_loop`, and `golish-recon-app` are current as of 2026-07-30 for Tool Truth Plan A: operation-frozen legacy-safe rollout, server-derived denominators, immutable exact receipts/raw lineage, closed TI/EAS/Enum/Vuln root taxonomy, target-state epoch + temporal policy authority, same-transaction multi-root checked/all-fresh guards, shadow-only Gate grades, current-attempt TargetIntel pinned transport, and held-by-default bounded revalidation obligations. Statuses remain ✅; this historical checkpoint predates the Plan C/D implementation recorded below.
- `golish-core`, `golish-agent-kit/harness`, `golish-db`, and `golish-db/repo` are current as of 2026-07-30 for Plan B identity and exact authority: canonical semantic/revision/comparison records, `plan_b_checked|grandfathered_legacy` whole-record basis, Plan A four-root/temporal/feed binding, H1/H2/checklist×partition closure, and raw semantic-summary exactness. Hash-only/prose authority and cross-basis comparison fail closed. Statuses remain ✅.
- `golish-agent-kit/task_orchestrator`, `golish-sub-agents`, `golish-sub-agents/defaults`, `golish-sub-agents/executor`, `golish-agent-runtime`, and `golish-agent-runtime/agentic_loop` are current as of 2026-07-30 for the typed two-wave Candidate Team: exact role ids `candidate_hypothesis_controller` / `candidate_hypothesis_analyst` / `merge_conflict_critic`, readonly submit-result-only execution, small-input 1 or otherwise 2–8 rolling live lanes, H1/H2 recursive ordering, and explicit non-PASS `AnalysisArtifactsReady`. Stage Team receipts remain control-plane only. Statuses remain ✅.
- `golish-agent-app`, `golish-agent-app/ai`, `golish-agent-bridge`, and `golish-agent-bridge/bridge_executor` are current as of 2026-07-30 for production dispatch/finalization: the app composition root installs the closed runtime Arc; operation-frozen policy selects Legacy/Registry before providers with no fallback; finalization is `same-RR pre-Gate material → pure Gate → apply transaction compiler seal + canonical generation + projection outbox`. Missing managed-feed authority still fails closed, but the runtime adapter is installed. Statuses remain ✅.
- `golish-agent-kit/db_traits`, `golish-agent-app/ai`, and `golish-db/repo` are current as of 2026-08-03 for the unified Investigation cognitive boundary: model output is advisory-only; the Investigation-owned compiler derives canonical hypothesis/verification authority and atomically seals generation plus automatic task admission; VerificationTask strategy uses the same dynamic PentAGI authority, receives only receipt-bound Campaign→objective UUID/hash mappings, and enters a zero-fixed-consult Campaign round before host-only Prepared Action compilation. Status remains integration WIP until the full entity run closes.
- `golish-agent-kit/db_traits`, `golish-agent-app/ai`, `golish-db`, and `golish-db/repo` are current as of 2026-08-03 for compound Investigation nested dispatch and per-VerificationTask Primary rearm: nested cognition remains advisory-only, while each task receives a receipt-authorized distinct WorkItem/WorkerRun/message chain behind exact previous-Primary/barrier/fingerprint fences and exact response-loss replay. These receipts constrain evidence and lifecycle authority; they do not replace the Main AI planner with a fixed DAG. Status remains integration WIP until the full entity run closes.
- `golish-db/repo` and `golish-sub-agents/executor` are current as of 2026-08-10 for restarted-client Company finalization: a closed final turn exposes only `submit_stage_deliverable`; if a later response-loss attempt exhausts the finalizer, no-purge rollover accepts only the same Worker's earlier-or-equal exact accepted submission receipt and still rejects absent/foreign submissions. Statuses remain ✅.
- `golish-core`, `golish-db`, `golish-db/repo`, `golish-agent-app/ai`, `golish`, `golish/app`, `golish/state`, `golish/cli`, `frontend/lib`, and `frontend/components` are current as of 2026-07-30 for projection/read closure: bounded opaque ids, canonical source/entity/change/event hashes, whole-batch replay, commit-visible NOTIFY + bounded polling process worker, exact principal/session/live-workspace/project/sealed-scope authorization, three readonly IPC wrappers, opaque v2 cursor/temporal consistency, and summary/list/detail UI loading/error/empty/stale/legacy-unavailable/residual states. Statuses remain ✅.
- `golish-agent-kit/db_traits`, `golish-agent-app/ai`, and `golish-agent-runtime/agentic_loop` are current as of 2026-08-02 for the authoritative Candidate→Campaign→JIT Prepared Action→trusted execution receipt→Oracle/objective closeout→revision adjudication→Wave fixed-point chain. Authoritative nonzero Candidate generations cannot use the historical Plan C-unavailable Reporting shortcut; runtime PASS requires durable closure at all three levels. Statuses remain ✅.
- `golish-db`, `golish-db/repo`, `golish-agent-kit/db_traits`, and `golish-agent-app/ai` are current as of 2026-08-14 for resolution-only asset closure provenance: an ordinary lane drains only when every current canonical root is backed by the exact dynamic-v2 round→Primary resolution→terminal transition/state-event→terminal head chain; fixed receipts freeze immutable per-root members plus an exact deferred count/set census, while legacy adjudication/server-validator closed heads remain backlog and tools/Campaigns/Waves remain audit-only.
- `golish-db/repo` and `golish-agent-app/ai` are current as of 2026-08-07 for Candidate temporal exact identity and downstream runtime compatibility: each Candidate temporal member and stale residual preserves its upstream temporal census member one-for-one even when one bundle member contains repeated observations; Application Understanding/Investigation telemetry uses the persisted compatibility role without changing business identity; anonymous-access probes only bind query names actually persisted in the endpoint URL. Status remains integration WIP until controlled and fresh-entity runs close.
- Plan D investigation DTOs, readonly Campaign/Timeline IPC and queue-centric workspace are implemented; generated TypeScript artifacts must continue to come from the `ts-rs` exporter rather than hand edits. Rollout promotion and safety-hold release remain local-admin operations.
- `golish-pentest-domain`, `golish-intel-providers`, `golish-recon-app/asset_intel`, `golish-recon-app/agent_tools`, `golish-agent-kit/tool_executors`, `golish-agent-kit/db_traits`, `golish-agent-app/ai`, `golish-agent-runtime/agentic_loop`, `golish-sub-agents/executor`, and `golish-agent-bridge/agent_bridge` are current as of 2026-08-02 for Target Intel Goal Shadow Tasks 2–4: non-TS semantic pivots, explicit safe adapter metadata, server-owned projection authority/audit receipt seams, and fake-only host public evidence tools. Production/historical operations remain legacy and `public_web_readonly` remains disabled. Statuses remain ✅.
- `golish-agent-kit/harness`, `golish-agent-kit/db_traits`, `golish-agent-runtime/agentic_loop` + `eval_support`, `golish-agent-app/ai`, `golish-sub-agents/executor`, `golish-db/repo`, and `scripts/run_tree.py` are current as of 2026-08-02 for Target Intel Goal production cutover: a V2 Target Intel Controller claim freezes the server-built `intel_goal_v1` contract/epoch; Gate PASS freezes the four-section bundle, dispatches a durable read-only reviewer, records its CAS verdict, and commits PASS only through the compound Intel + StageTeam finalizer. Org-scoped frontier, atomic findings/dispositions/holds, fake eval composition, and redacted run-tree summaries remain part of the same authority chain. Runtime/build verification is still required. Statuses remain ✅.
- `golish-agent-app/ai`, `golish-agent-runtime/agentic_loop`, and `golish/stage_run` are current as of 2026-08-10 for recovered unified Investigation acceptance: exact task-plan output manifests, checkpoint-only post-synthesis recovery, durable catalog admission, explicit Campaign safety-hold authority, root-only publication denominators, local methodology RAG/Memory evidence, and concise Reporting all closed in controlled-fixture and fresh moresec.cn Scoping→Reporting entity runs. GUI end-to-end remains a separate presentation check; entity closure must not be described as proof of every UI interaction. Statuses remain ✅.

- `golish-agent-kit/harness` and `golish-agent-app/ai` are current as of 2026-08-11 for EAS bounded port-producer residual closure: exact guarded `eas_discover_ports_policy` blocked outcomes close LIVENESS/PORT/SERVICE for concrete IPs and only LIVENESS/PORT for CIDRs; model-authored, foreign-source, wrong-class and mismatched-evidence blocked rows remain fail closed. Status remains ✅.

- `golish-db`, `golish-agent-kit/task_orchestrator`, `golish-agent-app/ai`, `frontend/store`, and `frontend/components` are current as of 2026-08-10 for pristine-install current-contract admission, restored-cursor-reachable Target Intel semantic-candidate scope review, the full-width fixed-height Stage Agent tree, message-scoped/persisted ChatPanel Stage Plans, resolution-only IP Target workbench selection, and WebContent memory stability: existing operations are not rewritten; passive observations remain candidate-only until explicit exact-window target review; stale provider kinds such as an IP literal labelled `domain` are filtered without discarding the correctly typed sibling; Stage/SubAgent cards converge on one organization/Controller/Worker call tree, detail focus keeps ChatPanel event projection alive without retaining its DOM, the selected-Agent transcript has a 200-entry presentation tail with a pinned Plan, and equal Target sets retain stable event listeners. Statuses remain ✅.
- `golish-tools/definitions`, `golish-agent-kit/db_traits`, `golish-agent-kit/tool_executors`, `golish-agent-kit/task_orchestrator`, `golish-sub-agents/defaults`, `golish-agent-app/ai`, `golish-agent-runtime/agentic_loop`, and `golish-db/repo` are current as of 2026-08-11 for Vuln anonymous-access exact-origin planning and formulaic Controller continuation: the current work-item origin selects the operation-bound endpoint manifest, bare-domain targets recover only a unique endpoint origin, ambiguous origins fail closed, the guarded wrapper reloads the exact eligible-id set before network send, historical Controller output is excluded from prepare-final manifests, and only exact open/sealed-finalizer recovery phases can reuse the singleton exhausted output. Fresh operation `5f7505a3-7081-432a-abfb-bad9c8dc8356` passed Vuln and completed through Reporting. Statuses remain ✅.

- `frontend/components` and `golish-pentest-app/pentest_bridge` are current as of 2026-08-11 for Codex-style typed Tool execution disclosure: adjacent ordinary tools collapse into readable activity groups; exact backend command/output/Job context and whitelisted Nuclei active-runner commands are available through nested disclosure; the in-process anonymous-access producer shows exact HTTP observations without fabricated curl; complete AI Tool Input/Result data stays one level deeper. Statuses remain ✅.
- `frontend/components` is current as of 2026-08-12 for retained Investigation presentation convergence: the exact Main/Analysis Primary alias has one selectable transcript, the bounded read session is a non-interactive context authority rather than a fake Agent, `passed` converges through the task tree, only the selected Verification path expands, canonical predicates and terminal submit payloads are secondary disclosures, and closed runs omit unavailable controls. Status remains ✅.
- `golish-agent-kit/db_traits`, `golish-db/repo`, `golish-agent-app/ai`, and `golish-agent-runtime/agentic_loop` are current as of 2026-08-11 for unified Investigation retained-child continuation: exact persisted WorkItem/dispatch/Refiner reads, read-only parked Primary authority, sealed Main read-session replay and deterministic child retry closed retained Moresec operation `4d5f17a5-88f5-423e-9dcb-3e9cad6e1003` through validated concise Reporting without another user continuation. Statuses remain ✅.
- The 2026-08-12 Primary-led Investigation milestone established same-history Analysis synthesis, zero-hypothesis handling, Tool Truth receipts and FactDelta/evolution authority. Its PreparedAction assignment/send runtime has since been physically removed; those tables and receipts are historical audit only, while current Verification uses asset-bound dynamic sessions, Tool Manager inventory/invocations and Primary resolution.
- The 2026-08-13 exact-bound-tool assignment injection path is likewise historical and no longer runnable. Current scope safety is enforced per dynamic invocation by exact asset lane/target/session/worker, JIT, budget, credential and evidence fences.
- `golish-sub-agents/executor` additionally documents the 2026-08-14 dynamic Asset Verification actor boundary: Verification Primary has a distinct exact session/lane/target/revision binding and a closed delegate-or-resolve turn; dynamic actor roles may repeat, while model citations remain limited to fresh session audit-evidence ids and all opaque authorities remain host-owned. Status remains ✅.
- `golish-core`, `golish-agent-kit/task_orchestrator`, `golish-agent-kit/tool_policy`, and `golish-agent-runtime/agentic_loop` are current as of 2026-08-12 for outer harness terminal-contract convergence: an accepted canonical `submit_stage_deliverable` capture is the sole Gate payload while final prose remains presentation-only, immutable scoped submission reload remains mandatory, and the server-scoped read-only `list_in_scope_targets` census is default-Allow/Low-risk in headless and GUI execution.
- `golish-agent-kit/db_traits`, `golish-db`, and `golish-db/repo` are current as of 2026-08-13 for the mandatory durable Investigation company/asset queue foundation: server-derived strict ordering, exact owner/CAS/replay fences, one active lane, persisted evolution fuel, company completion barriers, and zero-hypothesis-only fixed-point authority. Every new Investigation run must acquire this queue; historical organization-wide rows are audit-only and never a runtime fallback. Runtime/app wiring and lane-bound compiler backlog remain subsequent plan tasks.
- `golish-agent-kit/db_traits`, `golish-agent-kit/task_orchestrator`, and `golish-agent-app/ai` are current as of 2026-08-13 for the Investigation closure authority cutover: coordinator pass-token and Reporting source projection consume only the mandatory resolution-only company/asset queue publication, with exact operation/org/Unit/TeamPlan/hash/watermark/completion-time validation in one snapshot and no historical operation-global closure fallback.
- `golish-agent-kit/db_traits`, `golish-agent-kit/task_orchestrator`, and `golish-agent-app/ai` are current as of 2026-08-14 for the first legacy Campaign cutover atom: the orphan app scheduler and unused task-orchestrator Campaign state machine are physically removed, and the uncalled fixed-four Candidate→Campaign admission DTO/trait/app implementation no longer exists. Historical advisory, PreparedAction and Campaign persistence remain isolated for a later atomic cutover rather than being partially removed. Statuses remain ✅.
- `golish-agent-kit/db_traits` and `golish-agent-app/ai` are current as of 2026-08-14 for the second legacy Campaign cutover atom: the orphan Campaign scheduler progress/consult DTOs, all three unavailable default ports, their fixed-point unit test, and the loopback scheduler-only fixture/tail are physically removed. PreparedAction execution coverage remains intact and isolated for its later atomic cutover. Statuses remain ✅.
- `golish-agent-kit/db_traits` and `golish-agent-app/ai` are current as of 2026-08-14 for the fixed VerificationTask advisory cutover atom: subject/capability/strategy/action-intent/apply-resume/finalizer DTOs and host ports, the app materialization/finalization implementation, the advisory module, and their reservation-only tests are physically removed. Statuses remain ✅.
- `golish-core`, `golish-db`, `golish-db/repo`, `golish-agent-kit/db_traits`, and `golish-agent-app/ai` are current as of 2026-08-14 for compiler Campaign-reservation cutover: each new canonical root derives and admits one lane-bound `dynamic_v2` VerificationTask whose contract participates in its stable identity, writes no objective assignment/Campaign reservation, committed replay returns task IDs only and exact-rejects old assignment authority, while historical v1 rows remain append-only audit. Status is static-ready pending the coordinated focused migrated-PG run.
- `golish-agent-kit/db_traits`, `golish-agent-app/ai`, `golish-db/repo`, and `golish-agent-runtime/agentic_loop` are current as of 2026-08-14 for the assignment/send cutover atom: the unified Verification execution assignment DTOs/ports, app adapter/transport executor, DB runnable lifecycle writers, direct runtime driver, and dependent loopback fixture are physically removed. Historical assignment/Campaign schema and reporting remain read-only audit; new runs use asset-bound dynamic Verification sessions and Tool Manager invocations. Status is static-ready pending coordinated compilation.
- `golish`, `golish-agent-app/ai`, `frontend/lib`, and `frontend/components` are current as of 2026-08-14 for the PreparedAction manual-authority cutover: the exact-resume Campaign authority packet/CLI flag, Tauri list/decision commands and DTOs, attack API wrappers, generated PreparedAction types, and all PendingPreparedAction UI mounts/tests are physically removed. Historical Campaign/PreparedAction SQL, schema, DB-smoke exact sets and read-only Investigation projections remain audit-only; no compatibility execution path remains. Status is static-ready pending coordinated compilation.
- `golish-core` and `golish-agent-app/ai` are current as of 2026-08-14 for the same cutover: `allowPreparedActionJit` is physically removed from the frozen mode policy and Investigation projection IPC instead of being retained as a permanently false compatibility switch.
- `golish-agent-kit/db_traits`, `golish-agent-kit/harness`, `golish-agent-app/ai`, `golish-db/repo`, and `golish-pentest-app/pentest_bridge` are current as of 2026-08-14 for the orphan Campaign repository/shadow/compiler cleanup: zero-caller repository ports/adapters, shadow writer repo, fixed capability action compiler and rollout Campaign selector are physically removed. The production Candidate action tool, Nuclei Oracle reducer, shared `ComponentOracleOutcomeV1`, historical schema and reporting readers remain intact. Status is static-ready pending coordinated compilation.
- `golish-agent-app/ai` is current as of 2026-08-14 for scoped dead-code cleanup: the orphan Candidate chunk projection module and two zero-caller DB bridge helpers are physically removed; proposal-page validation is test-only under `cfg(test)` while the production Candidate compiler/Gate path remains unchanged. No warning suppression was added. Status is static-ready pending coordinated compilation.
- `golish-agent-app/ai` is current as of 2026-08-14 for Application Understanding all-target lint cleanup: disabled legacy Candidate/Campaign integration blocks and their exclusive localhost/reporting fixtures are physically removed, while the active AU runtime and typed stage-controller test suite remains intact. Status is static-ready pending coordinated compilation.
- `golish-db/repo` is current as of 2026-08-14 for retained Investigation TeamPlan cutover: one append-only effective-contract authority freezes source and effective material, then authorizes a single in-transaction canonical TeamPlan CAS only after an exact current fixed-roster receipt/census and no-live-I/O check; foreign, incomplete, stale, downgrade and repeated upgrades remain fail closed. Status is static-ready pending coordinated migrated-PG verification.
- `golish-db/repo` is current as of 2026-08-14 for scoped clippy structure cleanup: two dynamic Verification compound query results now use named `FromRow` authority structs, and identical legacy/dynamic governance replay skip branches share one short-circuit condition without changing error propagation. Status is static-ready pending coordinated compilation.
