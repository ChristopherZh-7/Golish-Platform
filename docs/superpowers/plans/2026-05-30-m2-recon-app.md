# M2 子计划：抽 `golish-recon-app`（recon 服务 crate）

> 父计划：`docs/superpowers/plans/2026-05-30-crate-per-service-split.md` §M2。
> 范式镜像 M1（`golish-vuln-app`）。本子计划按 writing-plans 写，分 **M2a / M2b / M2c** 三个各自可验证的子步。
> 用户指令「abc 一起搞」= 三子步全做。

**目标**：把 `golish/src/tools/` 下 recon 域 9 个模块组抽进独立 app crate `golish-recon-app`（L5.5），主二进制 `golish` 经 facade 重导出消费（层次 A：编译期依赖链）。ReconPort（S1-2b，切断上游硬依赖升层次 B）不在本计划范围。

---

## 0. 现状（自读核实，2026-05-31）

- `golish-recon-app` crate **已被上一会话脚手架化但未完成**：`Cargo.toml`（含 golish-app-core/db/core/settings/pentest/scan-runner/intel-providers/integrations + tauri/sqlx/reqwest/tokio/async-trait）+ `src/lib.rs`（声明 6 模块 `custom_rules/intel_providers/scan_queue/scan_runner/sensitive_scan/targets`，**但模块文件未搬入** → 当前不编译）。
- `backend/Cargo.toml` 已把 `golish-recon-app` 加入 members/default-members/workspace.deps。
- `check_dag.py` LAYER_TABLE **缺** `golish-recon-app` → 守卫当前 RED（unknown crate）。
- `golish/Cargo.toml` **缺** `golish-recon-app` 依赖。
- 全部未 commit（untracked），分支 `feat/recon-service` ahead 11。

## 1. 模块体检（自读 `use crate::` 实证）

| 模块 | 文件 | 跨 golish 耦合 | 结论 |
|---|---|---|---|
| targets | mod/cmds/db/directory/recon/types | error/state + `crate::tools::scoping`（IDOR）| M2a，scoping 下沉 app-core |
| organizations | mod/types/candidates/validation/tests | error/state | M2a |
| scan_queue | scan_queue.rs | error/state；读 vuln `scan_queue` 表（allowlist）| M2a |
| sensitive_scan | sensitive_scan.rs | error/state + golish_pentest + golish_db | M2a |
| custom_rules | custom_rules.rs | error/state | M2a |
| scan_runner | scan_runner/mod | error/event_emitter/state + golish_scan_runner；调 `crate::tools::targets::db_directory_entry_add`（域内）| M2a |
| intel_providers | intel_providers.rs | error/state + golish_pentest + golish_intel_providers + golish_db | M2a |
| **asset_intel** | mod + 12 子文件 | error/event_emitter/state + `crate::tools::organizations`（域内）+ **`crate::tools::pentest::PentestState`（阻塞）** | M2b |
| integrations | mod + commands/state/capture/* | `IntegrationsState` + tauri webview + golish `tauri_app.rs` 注册 | M2c |

**入向（golish 留守模块读 recon 模块）**：
- `tools/pipeline/storage.rs`（pentest，留守）→ `crate::tools::targets::db_target_add` ⇒ golish 需 `targets` 再导出桥。
- `tools/asset_intel/*`（M2b 才搬）→ `crate::tools::organizations` ⇒ M2a 期间 golish 需 `organizations` 再导出桥（M2b 后 asset_intel 入 recon-app 变域内）。
- 其余 5 模块无 golish 留守消费者。

## 2. 共享件下沉：`scoping` → `golish-app-core`

`golish/src/tools/scoping.rs`（33 行 IDOR 守卫，仅依赖 `GolishError`）被 6 处用（notes/pipeline/vault/findings/methodology/**targets**）。targets 要搬走 → scoping 下沉 `golish-app-core`：
1. 新建 `golish-app-core/src/scoping.rs`（内容同，`use crate::GolishError`，fn 提 `pub`）。
2. app-core `lib.rs` 加 `pub mod scoping;`。
3. 删 `golish/src/tools/scoping.rs`；`tools/mod.rs` 把 `pub(crate) mod scoping;` → `pub(crate) use golish_app_core::scoping;`（golish 5 处留守调用 `crate::tools::scoping::*` 仍解析）。
4. recon-app targets/cmds.rs：`crate::tools::scoping::*` → `golish_app_core::scoping::*`。

---

## M2a —— 7 个干净模块

1. `git mv` 7 模块文件 → `golish-recon-app/src/`（targets/ 6 文件、organizations/ 5 文件、scan_queue.rs、sensitive_scan.rs、custom_rules.rs、scan_runner/、intel_providers.rs）。
2. recon-app `lib.rs` 加 `pub mod organizations;`（其余 6 已声明）。
3. 导入重映射（所有搬入文件）：`crate::error::GolishError`→`golish_app_core::GolishError`；`crate::state::DbState`→`golish_app_core::DbState`；`crate::event_emitter::TauriEventEmitter`→`golish_app_core::TauriEventEmitter`；`crate::tools::scoping::*`→`golish_app_core::scoping::*`；`crate::tools::targets`→`crate::targets`（scan_runner 域内）。
4. golish 接线：`Cargo.toml` 加 `golish-recon-app = { workspace = true }`；facade `workspace.rs` 6 行 + `intel_providers.rs` 改指 `golish_recon_app::<mod>::*`；`tools/mod.rs` 删 7 模块声明，加 `pub(crate) use golish_recon_app::{organizations, targets};`（入向桥）；按编译报错把 `upsert_organization_candidates_for_org`/`db_target_add` 等提 `pub`。
5. 守卫：`check_dag.py` LAYER_TABLE 加 `golish-recon-app: 5.5`；`check_repo_ownership.py` SOURCE_ROOTS 加 `("golish-recon-app","recon")`，迁移 ALLOWLIST/RAW_SQL_ALLOWLIST 键（`tools/X`→`golish-recon-app/X`：scan_queue/sensitive_scan/custom_rules/intel_providers/targets/{cmds,db,directory}）。
6. **验证**：`cargo check -p golish-recon-app` + `cargo check -p golish` + `check_dag.py` + `check_repo_ownership.py` 全 exit 0。

## M2b —— asset_intel（解 PentestState）

`asset_intel` 唯一坏耦合 `crate::tools::pentest::PentestState`，窄用：取 `config_manager`(=`golish_pentest::ConfigManager`) → toolsconfig_dir → `golish_pentest::scan_toolsconfig`。
- 解法：把 asset_intel 用到的 PentestState 字段收窄为 recon-app 可直接构造/接收的 `golish_pentest` 类型（共享 `Arc<ConfigManager>` 受管状态或窄 `ToolsConfigPort`），命令签名改收窄类型；golish 注册侧注入。
1. `git mv tools/asset_intel/` → recon-app；`lib.rs` 加 `pub mod asset_intel;`。
2. 导入重映射同上；`crate::tools::organizations`→`crate::organizations`（域内，撤 organizations 桥）；PentestState 解耦改写。
3. facade `asset_intel.rs` 改指 `golish_recon_app::asset_intel::*`；`tools/mod.rs` 删 asset_intel 声明 + 撤 organizations 桥。
4. 守卫迁移 `tools/asset_intel/runtime/mod.rs` RAW_SQL 键。
5. **验证**同 M2a。

## M2c —— integrations（webview + 启动接线）

`integrations` 自带 `IntegrationsState` + tauri webview（WebviewWindowBuilder/Wry）+ golish `app/tauri_app.rs` 注册 CaptureEngine/IntegrationsState。
1. `git mv tools/integrations/` → recon-app；`lib.rs` 加 `pub mod integrations;`。
2. 导入重映射；状态/capture engine 随之搬；golish 启动处（`app/tauri_app.rs` `.manage(...)`）改指 `golish_recon_app::integrations::*`。
3. facade `integrations.rs`：改 glob `golish_recon_app::integrations::*`（或显式含 `__cmd__`）。
4. `tools/mod.rs` 删 integrations 声明。
5. **验证**同 M2a + 关注 webview 编译。

---

## 3. 验证 & 回滚（全局）

- 每子步：`cargo check -p golish-recon-app && cargo check -p golish`；`python3 scripts/check_dag.py`；`python3 scripts/check_repo_ownership.py`（全 exit 0）。收尾 `just precommit`（AGENTS.md §3）。
- 纯结构性搬移 + facade 转发 + 入向桥；未跑通前不删旧路径并存。
- 高风险（§2.7）：新增 crate 依赖 / 命令注册面（全量 IPC）/ tauri_app 启动接线 → 不 commit/push，等用户点头。

## 4. 自检

- 抽取顺序遵 DAG（vuln 叶子已抽 → recon），recon-app 仅依赖 ≤L5.5，无环。✓
- 入向 targets/organizations 用编译期桥（层次 A）；ReconPort 升 B 留 S1-2b。✓
- scoping 下沉 app-core 消除 targets 的 `crate::tools` 依赖。✓
