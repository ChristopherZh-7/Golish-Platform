# Persistent exact Campaign authority plan

1. Remove only the retained-pgdata requirement from the existing Campaign
   authority CLI argument; retain all exact resume selectors.
2. Apply the unchanged packet after exact resume identity/advisory validation
   for either database source; preserve the Investigation-only stage guard.
3. Update the stage-run module card and focused parser/packet tests.
4. Build the apex binary once, apply a hash/CAS-bound hold-release packet to the
   retained moresec operation, and resume the same operation to Reporting.
