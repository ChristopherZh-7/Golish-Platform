[package]
name = "golish-vuln-intel-domain"
version.workspace = true
authors.workspace = true
edition.workspace = true
description = "Vulnerability intelligence domain models and pure logic — no I/O dependencies"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
