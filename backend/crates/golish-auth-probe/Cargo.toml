[package]
name = "golish-auth-probe"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "API authorization probe — runs 3-round controlled requests against extracted endpoints to detect anonymous-access / cross-user IDOR / privilege-escalation vulnerabilities"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
reqwest = { workspace = true }
tokio = { workspace = true }
url = { workspace = true }

# Reuse Endpoint / UrlKind / AuthHint definitions from the analyzer.
golish-js-analyzer = { workspace = true }
